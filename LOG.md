# LOG

Короткая запись по каждому крупному шагу: что сделано, что сработало, что нет.

---

## 2026-08-05: бот intrade.bar — Слой 1 (клиент API)

Ветка `feature-intrade-bot` (worktree `/root/projects/terminal-bot`), в master
не сливалась. Строится ОБОЛОЧКА по `BOT_TZ.md`; стратегии в ней нет и не
планируется в этой задаче.

**Сделано:**
- `INTRADE_API_MAP.md` + `BOT_TZ.md` в репозиторий; `intrade_bot_plan.md`
  удалён как устаревший (писался до разведки: выдуманный эндпоинт
  `/api/trade/open` с JSON, авторизация по cookie, ни слова о задержке
  открытия; источником сигналов считал выключенный `signal_engine`).
- Пакет `bot/`: `config.py`, `clock.py`, `run.py`, `api/{models,parsers,
  client,auth}.py`. Python 3.10, `core/` не тронут (проверено `python3.7 -m
  compileall core/`).
- Тесты: `tests/test_bot_parsers.py` (51 проверка), `tests/test_bot_config.py`
  (21 проверка). Разбор сделки — на СОХРАНЁННОМ реальном ответе площадки
  (`tests/fixtures/intrade/trade_open_ok.html`), не на выдуманном.

**Проверено живьём с VPS (без VPN-контейнера, прямым выходом):**
- `intrade.bar/price_now` — HTTP 200 за 111–121 мс, 26 пар. Все четыре наши
  пары (USD/JPY, EUR/USD, AUD/USD, USD/CAD) у площадки есть.
- `intrade35.bar` — HTTP 200 за 246 мс.
- `wss://intrade35.bar/req_info` — работает, шлёт `{"Time_server":…}` раз в
  секунду. **Расхождение часов VPS с площадкой ≈ 0** (−0.15 с с учётом
  полусекундной поправки на гранулярность). Отдельная синхронизация всё равно
  оставлена: часы VPS могут уплыть.

Итого IP франкфуртского VPS площадка принимает напрямую — отдельный VPN-контур
боту не нужен.

**Решения, которые стоит помнить:**
- Открытие сделки НЕ ретраится никогда: ответ мог дойти, повтор = двойная
  ставка. При обрыве поднимается `TradeUnknown`, разрешать — сверкой через
  `user_real_trade.php`. Ретраи разрешены только на чтении.
- Неизвестный режим в конфиге сводится к `dry`, а не к торговле; `live`
  требует ещё и `INTRADE_ALLOW_LIVE=yes` в окружении (тестом закреплено).
- Разбор — регулярками, а не BeautifulSoup: время экспирации лежит в
  аргументах `setInterval` внутри `<script>`, куда HTML-парсер не заглядывает.
- `bot_config.example.json` пришлось внести в `.gitignore` исключением
  (`!bot_config.example.json`): общее правило `*.json` скрывало пример конфига,
  как когда-то `retention.json`.

**Приватная часть API проверена живьём (хеш получен вечером 05.08):**
- `balance.php` → **9363.20 $** (в HAR при разведке было 9363,08 — счёт тот же).
- `ajax_percent.php` USD/JPY → **82%**, безубыточный винрейт 54.9%.
- **Гипотеза разведки подтверждена: cookie не нужны вовсе.** Голая пара
  `user_id` + `user_hash` в полях формы авторизует приватные вызовы.
  Playwright боту не нужен — ни постоянно, ни для переподключения.

`.env` ветки — СВОЙ, отдельный от боевого (`/root/projects/terminal/.env`
читают `chart-feed` и брифинг; ключи площадки там были бы лишней поверхностью
утечки). Права 600. Следствие: `bot.run` надо запускать из каталога worktree,
иначе подхватится боевой `.env` без ключей intrade.

**Открытый вопрос, ещё не закрытый:** срок жизни `user_hash`. Хеш получен
2026-08-05 ~19:00 UTC+5 и в этот момент рабочий. Проверить `python3.10 -m
bot.run check` через сутки (2026-08-06) и через неделю; результат записать
сюда в любую сторону. Если протухает за часы — придётся возвращаться к
Playwright, и это меняет схему эксплуатации.

**Сетка выплат (владелец + живая проверка, `bot/payout.py`):**

| Экспирация | Ставка | Выплата | Безубыточный WR |
|---|---|---|---|
| 1 и 3 мин | < 80 $ | 82% | 54.9% |
| 1 и 3 мин | ≥ 80 $ | 85% | 54.1% |
| 4–500 мин | < 80 $ | 79% | 55.9% |
| 4–500 мин | ≥ 80 $ | 85% | 54.1% |

Порог «5000 рублей» на долларовом счёте = **ровно 80 $, включительно**.
Мой первый замер (1 и 100) дал грубое «около 100»; владелец проверил руками
и указал 80, перебор с точностью до цента подтвердил: `79.99 → 82%`,
`80.00 → 85%`. Урок: границу искать перебором, а не двумя точками по краям.

**⚠ Окно у начала часа — главная находка дня. ПОДТВЕРЖДЕНО ЖИВЫМ ЗАМЕРОМ.**

Проба `ajax_percent.php` раз в 30 с вокруг стыка часа (USD/JPY, 1 мин,
ставка 1), 20 замеров — предсказание модуля сошлось на ВСЕХ:

| Время МСК | Выплата |
|---|---|
| 22:55:25 – 22:56:56 | 82% (вне окна) |
| **22:57:26 – 23:02:58** | **60% (окно)** |
| 23:03:28 – 23:04:28 | 82% (вне окна) |

Границы ровно ±3 минуты, как и было сказано: падение между 22:56:56 и
22:57:26, возврат между 23:02:58 и 23:03:28.

За 3 минуты ДО и 3 минуты
ПОСЛЕ начала часа выплата падает до **60%**, действует с 17:00 до 09:00 МСК.
Безубыточный винрейт при этом прыгает с 54.9% до **62.5%** (+7.6 п.п.).
Стратегия с WR 58% прибыльна в обычное время и убыточна в окне; это 96 минут
в сутки, 10% времени. Ограничитель обязан резать такие входы ДО отправки
запроса. Границы окна покрыты тестами поминутно, включая переход через
полночь (`tests/test_bot_payout.py`, 49 проверок).

**Ноль вместо процента.** Ночью Classic-экспирации (5, 60 мин) отдают `0%` —
так площадка сообщает «сейчас не торгуется» (ср. `error_time_night`). Ноль
трактуется как запрет входа, а не как сбой разбора.

**НЕ проверено (требует реальной ставки, т.е. Слоя 7):** открытие сделки,
формат `trade_check2.php` на расчёте, стабильность задержки ~3 с. Парсер
расчёта при неожиданной форме возвращает `unknown`, а не догадку.

### Слои 2–5: журнал, машина состояний, ограничители, контракт стратегии

Сделано в тот же день. Все тесты офлайн — площадка подменяется заглушкой,
которая умеет обрывать связь и отказывать по заказу.

- **Слой 5 (контракт) написан раньше срока** — ТЗ ставит его пятым, но само
  же требует «проектировать сразу» (§6). Причина: `journal.py` обязан писать
  `Signal.meta`, а `engine.py` — принимать источники; без контракта схему БД
  и ядро пришлось бы согласовывать задним числом. Критерий ТЗ закрыт тестом:
  `FakeStrategy`, объявленная прямо в тесте и неизвестная оболочке, работает
  без единой правки ядра. Отдельная проверка обходит `bot/` и следит, что
  вне `strategy/` никто не импортирует конкретные стратегии.
- **Слой 2 (журнал).** Таблицы `trades` и `events` в отдельном файле,
  `market.db` не трогается. Ошибка БД логируется и проглатывается — уронить
  торговлю из-за журнала хуже, чем потерять запись (закреплено тестом на
  закрытой БД).
- **Слой 3 (машина состояний).** IDLE → VALIDATING → SENDING → OPEN →
  SETTLING → CLOSED плюс REJECTED / FAILED / UNKNOWN. Режим `dry` моделирует
  задержку 2.8–3.6 с: имитация без задержки была бы отладкой в условиях,
  которых не существует.
- **Слой 4 (ограничители).** Девять проверок, каждая отдельным тестом.
  Стоп по серии убытков и по дневной просадке НЕ снимается сам — только
  `release()` руками.

**Два бага, пойманных собственными тестами:**

1. `int()` вместо `round()` в расчёте `latency_ms`: на потере точности float
   (`base + 2.8` = 2799.9997) задержка систематически занижалась на 1 мс.
   Исправлено в трёх местах.
2. Счётчик `max_concurrent` не освобождался при исключении в расчёте — бот
   замолчал бы навсегда после первой ошибки. Освобождение перенесено в
   `finally`.

**Ограничение метрики, выяснившееся на тесте:** `latency_ms` принципиально
огрублён до **±1 секунды** — площадка отдаёт `data-timeopen` целыми
секундами. Осмысленны выводы уровня «медиана около 3 с» и «на волатильности
выросла вдвое»; отличить 3.1 с от 3.4 с на одной сделке нельзя, но медиана
по 20+ сделкам показательна.

### Слой 6: панель наблюдения (:8788)

`bot/panel.py` + `bot/static/panel.html`, отдельный сервер. `index.html` и
`hub.py` не тронуты — прецедент `dashboard.html` подтвердился.

**Баг, стоивший времени:** страница отдавалась ПУСТОЙ (HTTP 200, 0 байт).
`websockets.Response` принимает тело только конструктором, а присваивание
`response.body` после `connection.respond()` молча ничего не делает —
ни исключения, ни записи в лог. Тест теперь проверяет, что тело > 5 КБ.

Адрес вынесен в `panel_host` (умолчание `127.0.0.1`). Режим `live` с
панелью на `0.0.0.0` **запрещён на уровне валидации**: у панели нет пароля,
и реальные деньги с открытой кнопкой Call не должны сойтись даже случайно.

Живая проверка: панель открывалась наружу в режиме `dry`, владелец нажал
PUT и CALL — обе сделки прошли полный цикл (открытие с задержкой ~3 с,
расчёт через минуту, запись в журнал). Обе в минус, но это ничего не
значит: в `dry` исход определяет случайное движение цены, стратегии нет.

### Слой 8: каркас `hub_feed`

`bot/strategy/hub_feed.py` — подписка на WS хаба без логики направления.
Точка подключения стратегии — `_evaluate()` / параметр `decide`.

**Три ловушки протокола хаба, все найдены живой проверкой** (каждая
выглядела как «соединение есть, данных нет» — без ошибок в логе):

1. Подписка идёт ключом **`type`**, а не `action`. Хаб на незнакомый ключ
   не отвечает ошибкой — просто игнорирует, а широковещательный поток
   продолжает идти, создавая видимость работы.
2. Нужен **`max_size=None`** у `websockets.connect`: умолчание 1 МБ, а хаб
   на `set_tf` отдаёт окно 10 080 свечей (M1) и рвёт соединение кодом 1009
   ДО первой свечи. Симптом обманчив — выглядит как нестабильная сеть.
3. История лежит в поле **`data`**, а не `candles`.

**Подписка у хаба живёт НА СОЕДИНЕНИИ:** второй `set_tf` в том же сокете
заменяет первый (`hub.py:_on_set_tf`). Для нескольких пар нужно соединение
на пару — фронт делает так же (вторая панель открывает свой сокет).

Проверено против боевого хаба: история 10 080 → буфер 50, живые свечи по
обеим парам, заглушка стратегии вызвана 4 раза за 45 с на S15, сигнал
дошёл через интерфейс с заполненным `meta`.

Детектор всплесков с ветки `hypothesis` НЕ подключался — §16 ТЗ запрещает,
и разумно: сначала надо измерить задержку площадки. Если она на
волатильности растёт, минутная стратегия неприменима в принципе.

**Итого оболочка готова: слои 1–6 и 8, тестов 381, все зелёные.**
Остался Слой 7 — живой прогон на демо и замер задержки на 20+ сделках.

### ⚠ 2026-08-06: площадка НЕ сообщает, какой счёт активен

Самая опасная находка проекта. Обнаружена случайно: `bot.run check` показал
баланс **11.56 $** вместо привычных 9363.20 $. Сначала решил, что площадка
сбросила демо; владелец объяснил — **он переключился на РЕАЛЬНЫЙ счёт**.

Что выяснилось при разборе:

- `balance.php` **один на оба счёта** и отдаёт баланс того, который выбран
  в кабинете. Отдельных `balance_demo.php` / `user_demo_trade.php` нет — 404.
- **`user_hash` при переключении НЕ меняется.** Тот же хеш, тот же
  `user_id`, а деньги уже настоящие.
- intrade.bar не даёт держать демо и реал открытыми одновременно для
  одного аккаунта — даже из разных браузеров.
- Наблюдался кэш: один запрос вернул 9363.20 $, следующие три подряд —
  11.56 $. То есть даже по сумме определить счёт можно лишь приблизительно.

**Чем это грозило.** Вся защита `live` строилась на поле `mode` в конфиге.
Но какой счёт активен — решает ПЛОЩАДКА, а не наш конфиг:

    mode: "demo"  +  на площадке активен реал  =  РЕАЛЬНЫЕ СТАВКИ

Три рубежа защиты `live` при этом спокойно спят: с их точки зрения режим
демо. Запусти я Слой 7 по плану (20+ сделок по 1 $) — прогон ушёл бы на
живой счёт с балансом 11.56 $ и съел его целиком.

**Что сделано.** `Engine._verify_account()` — последний рубеж перед каждой
реальной ставкой: если `account="demo"`, баланс сверяется с порогом
`min_balance_for_demo` (1000 $ по умолчанию). Ниже порога — отказ, ставка
не уходит. Не смогли узнать баланс — тоже отказ: слепая ставка при
неизвестном счёте недопустима.

Признак грубый (демо исчисляется тысячами, реал у владельца — единицами),
но это ВСЁ, что даёт площадка. Проверка делается перед каждой ставкой, а
не раз при старте: счёт можно переключить посреди прогона.

Проверено на живых данных: бот увидел 11.56 $, распознал реальный счёт и
отказался ставить — ноль попыток открытия.

**Побочный эффект, тоже полезный:** добавление обязательного вызова
`balance()` уронило все прежние тесты режима `demo` — у заглушки площадки
такого метода не было. Это ровно тот случай, когда падение тестов
показывает, что защита действительно встроена в путь исполнения, а не
приделана сбоку.

**Для Слоя 7 остаётся открытым:** на каком счёте вести замер. Демо
безопаснее, но требует переключения в кабинете; реальный счёт с 11.56 $
для 20 сделок по 1 $ не годится — серия убытков оборвёт выборку.

## 2026-08-04: Bloomberg API → брифинг

**Сделано:**
- `briefing/sources_bloomberg.py` — новый модуль, `fetch_bloomberg_news()`:
  ходит в Bloomberg через RapidAPI (`bloomberg-finance.p.rapidapi.com`),
  забирает `/news/list` по категориям `phx-markets/currencies/economics/latest`,
  фильтрует по тем же FOREX/CRYPTO/VENUE-ключевикам что и RSS.
- API — ТОЛЬКО новостной: `/market/*` и `/stories/*` на нём нет (проверено).
  В ответе каждая story уже содержит `autoGeneratedSummary` (полный абзац) и
  `published` (unix ts) — detail-запрос не нужен.
- `run.py` — вызов Bloomberg перед RSS, мерж (Bloomberg-аннотации вперёд).
- `prompt.py` — без изменений (рыночных данных Bloomberg не даёт, блок не нужен).
- Модель брифинга: `deepseek-v4-pro` → `deepseek-chat` (flash), быстрее.
- Ключ `RAPIDAPI_KEY` в `.env`.

**Результат живого теста:** 16 сырых → 4 релевантных (Treasuries, Moderna trial,
Madagascar rate hike, Wall Street crypto). Фильтр работает, формат ответа
разобран правильно.

**Не сделано (API не позволяет):** курсы валют, аналитика по тикерам —
эндпоинтов `/market/*` и `/stories/*` нет на `bloomberg-finance`.

## 2026-08-03 — автобрифинг снова в кроне

По запросу владельца раскомментированы три крон-задачи брифинга:
`0 4/12/17 * * * python3 -m briefing.run` (pre-Asia/London/NY, UTC+5).
Хаб и фронт не трогал — они и так живые.

## 2026-07-28 (вт) — Дашборд отдельной страницей (dashboard.html)

**Что сделано**

Новая страница `dashboard.html` по макету владельца (тёмный крипто-дашборд).
`index.html` и `hub.py` НЕ тронуты — ни строки: дашборд говорит с хабом по
существующему протоколу.

Блоки: крупный график (клик — разворот на весь экран для анализа, ТФ M1–D1,
Escape/клик по фону — закрыть), плитки инструментов, таблица «Обзор рынка»
(цена, 24ч, объём, открытый интерес, фандинг, сделки), лента крупных сделок,
карточка инструмента с полосой дневного диапазона, брифинг, каркас-заглушка
под будущую панель бота.

**Ключевое решение — по соединению на график.** Хаб держит ОДИН символ на
сокет (`hub.py:_on_set_tf` перезаписывает запись клиента), поэтому
мультиплексировать один сокет нельзя: второй `set_tf` молча отпишет от
первого символа. Сделано: общий сокет под широковещательные типы
(`instruments`/`ticker`/`large_trade`/`briefing`) + свой сокет у каждого
графика (класс `ChartFeed`). Альтернатива — мультиподписка в хабе — отвергнута:
это правка живого прод-процесса ради страницы, которая обходится без неё.

Данных о счёте/портфеле (половина макета: стейкинг, транзакции, награды) у
терминала нет вообще. Вместо выдуманных цифр — наши реальные: сводка биржи,
лента сделок, брифинг. Блок бота — честная пустая заглушка.

**Что работает (проверено вживую, headless chromium против :8082)**

Ноль JS-ошибок и упавших запросов. 8 плиток, 12 строк рынка, лента сделок,
брифинг. Разворот графика: живая цена, смена ТФ, закрытие — при закрытии
сокет и график уничтожаются (`ovCanvasAfter: 0`, утечки нет). Форекс и крипта
обе ветки: FXCM честно пишет «нет биржевой сводки» вместо прочерков.

**Грабли**

- Брифинг сперва вывелся сырым JSON: у `briefing.json` нет поля `text`, там
  `pairs → {direction, deepseek_view}` + `crypto` + `global_context` +
  `track_record`. Переписано под реальную структуру, блок зависит от
  выбранного инструмента (форекс → своя пара, крипта → общий `crypto`).
- Проверка через `textContent` показала «слипшиеся» цену и процент — это
  артефакт чтения текста, а не вёрстки: замер `getBoundingClientRect` дал
  зазор 5 px. Мораль: визуальные баги мерить геометрией, а не строками.
- `cheap-llm` на ревью 35-КБ файла вернул пустой ответ — по политике не
  ретраил, проверил сам прогоном в браузере (надёжнее текстового ревью).

**Открыто**

Панель бота — когда появится источник данных о сделках. Плитки показывают 8
инструментов из 12 (BTC дублирует крупный график) — при желании расширяется.

---

## 2026-07-22 (ср) — Стакан, карточка инструмента, крипта в брифинге

**Что сделано**

1. **Стакан — живая гистограмма лимитных заявок** (13 коммитов, в проде).
   Стены bid/ask справа от графика, столбцы по сетке ценовой шкалы.
   Отключаемый индикатор, ничего не хранится.

   Четыре слоя: контракт шины + обратный канал хаб→фид (`89f8c8a`),
   локальная книга заявок (`5863b6c`), хаб-ретранслятор (`c17e848`),
   canvas-оверлей (`f907ff3`).

   Ключевые решения:
   - подписка у биржи ТОЛЬКО пока индикатор включён — она стоит ~1.1 ГБ
     в сутки на инструмент (замер: 16.7 обновлений/сек);
   - `size=0` у Lighter означает СНЯТИЕ уровня, а не нулевой объём.
     Проверено живьём: за минуту 1048 дельт, из них 4157 снятий — приняв
     их буквально, книга забилась бы мёртвыми ценами;
   - склейка уровней в корзины во ФРОНТЕ, не на сервере: размер корзины
     зависит от зума, и только данные в браузере дают перерисовку в том же
     кадре, что и график;
   - срез ±2% от mid (сначала было ±0.5% — покрывало лишь 45% видимой
     шкалы, стены жались к центру).

2. **Карточка инструмента** (`f7983d8`). Кнопка ⓘ: mark/index price, 24h
   change/high/low, объём, число сделок, открытый интерес, фандинг.
   Обновление раз в 5 с. Ставка фандинга берётся ТОЛЬКО `exchange=lighter`:
   в ответе API лежат ещё binance/bybit/hyperliquid по тем же рынкам, и по
   BTC они расходятся втрое. OI приходит в базовой валюте — переводится в
   доллары по mark price.

3. **История старших ТФ на крипте** (`ffeeaa7`). Была 1.5 дня на ВСЕХ ТФ
   против 406 дней D1 у FXCM: фид грузил только 1m (33 часа), а старшие
   собирались из него. Теперь каждый ТФ берётся у биржи напрямую, зеркально
   FXCM. Стало: D1 — 499 дней, H4 — 83, H1 — 62.5, M15 — 15.6.

4. **Крипта в брифинге** (`0152de0`). 6 живых крипто-фидов, три категории
   новостей (forex/crypto/venue), общий блок «крипторынок» в промпте.
   Расписание прежнее. Новости про биржу Lighter идут вне квот и помечены
   `[БИРЖА LIGHTER]` — они бьют по всем инструментам разом.

**Что ловилось по дороге**

- **Срез стакана отсекался фильтром `requestId`** (`db88cea`) — цепочка
  работала до самого браузера, где сообщение молча отбрасывалось. Тот же
  баг, что раньше съедал `instruments`, и комментарий об этом стоял
  строкой выше. Цепочка из девяти `msg.type !== ...` заменена на явный
  набор `BROADCAST_TYPES`.
- **Кнопки стакана не работали** (`01975fe`): разметку добавил, CSS забыл,
  и меню не звало синхронизацию — строка не отражала ни доступность, ни
  положение тумблера.
- **Фильтр крипто-новостей врал**: поиск подстрокой находил «ETH» внутри
  «Hegseth», «SEC» внутри «Secretary», «Optimism» в «investor optimism».
  85 ложных совпадений из 174. Перевёл на границы слов.
- **Столбцы bid/ask слипались на спреде**: сторона выводилась из цены
  корзины, а корзина шире спреда и накрывает обе стороны сразу.

**Уроки**

- Broadcast-тип, забытый в `BROADCAST_TYPES`, теряется МОЛЧА. Это уже
  третий случай (instruments, orderbook, ticker) — теперь набор явный и
  с объяснением.
- Короткие тикеры нельзя искать подстрокой. Ни один тест этого не ловил,
  нашлось только живым прогоном на реальной ленте.

---

## 2026-07-21 (вт) — Фаза 3 влита в прод + два бага, найденных вливанием

**Что сделано**

Ветка `phase-3-lighter` (21 коммит) влита в master (`5ded560`). Установлен и
включён юнит `chart-feed-lighter` — он лежал готовым в `deploy/` с Фазы 3,
запускать в проде было нечем. Прод: 4 сервиса, 15 инструментов живьём
(4 форекс + 11 крипто), 0 ошибок в логах.

**Двухфидовый прогон оказался невозможен — и это стоило бага**

Двое суток ждали «прогон с двумя фидами перед вливанием». Проверка
неисполнима: демо-счёт FXCM даёт ОДНО подключение к брокеру, занятое боевым
фидом; пустить живой форекс через тестовый хаб нечем, не погасив прод.
Попытка обойти мостом (перелив из боевого хаба в тестовую шину) — тупик:
хаб отдаёт уже нарезанные свечи, мост проверял бы суррогат, а не сырой путь.

Вливание нашло за 30 секунд то, что обкатка не нашла за 48 часов:

1. **`_sum_volume` падал на свечах FXCM из БД** (`d188cde`). Проверял
   `key in source` — НАЛИЧИЕ ключа. Свечи из памяти ключей объёма не имеют,
   и обкатка на одной крипте это не ловила. Но sqlite возвращает ВСЕ колонки:
   у 103 135 строк FXCM `delta` приходит как NULL → None. Ключ на месте,
   проверка проходит, дальше `0.0 + None` — и каждое сообщение шины,
   задевавшее старший ТФ, гибло с TypeError. Заменено на
   `source.get(key) is not None`, добавлены тесты на данные «как из БД».

2. **Брифинг не видел ключ DeepSeek из крона** (`9c70cea`), мёртв с 18.07.
   `DEEPSEEK_API_KEY` читался в КОНСТАНТУ МОДУЛЯ, т.е. при импорте. `run.py`
   импортирует `briefing.agent` на строке 25, а `load_dotenv()` зовёт на 30 —
   на момент импорта `.env` не прочитан, константа замерзала пустой. Вручную
   не воспроизводилось: там ключ в окружении ДО импорта. Три провала в сутки,
   всегда на одной строке. Ключ читается в момент вызова; проверено запуском
   в `env -i` (пустое окружение, как у крона).

**Уборка**

- Обкатка (`soak_*`, 4 процесса, 2 сут 18 ч) дублировала прод — качала с
  Lighter то же самое, что боевой фид. Остановлена.
- `scratchpad`: 321 МБ → 932 КБ. Бэкап `market.db` перед вливанием перенесён
  в `archive/phase3_merge_backup/`, чтобы пережил очистку `/tmp`.
- Проверено: кольцо тиков работает (крипта — только сегодня), форекс-архив
  под ротацию не попадает намеренно, крон чист, посторонних процессов нет.
- НЕ тронуто: `vel_log/` (226 МБ) и `History/` (31 МБ) — мёртвый груз с 15.07,
  но `vel_log` числится в CLAUDE.md как «never delete». Ждёт решения владельца.

**Уроки**

- Тестовый контур на ОДНОМ провайдере не проверяет код, общий для двух.
  Обкатка была честной и длинной, но структурно слепой к половине путей.
- Если проверка неисполнима — это надо признать сразу, а не держать пункт
  открытым двое суток. Я держал, и это ничего не дало.
- Свежий процесс ≠ свежий код: soak-хаб два дня исполнял код на момент
  старта, из-за чего фикс алертов «не работал» (см. [[soak-hub-holds-stale-code]]).

---

## 2026-07-19 — Фаза 3: Lighter как источник данных (ветка `phase-3-lighter`)

**Что сделано**

12 инструментов Lighter рядом с 4 парами FXCM: BTC, ETH, HYPE, SOL, WTI, LIT,
ZEC, XAU, BNB, SPCX, MU, XAG. Ключи не нужны — рыночные данные публичны.

- `feeds/lighter_feed.py` + `feeds/lighter_raw.py` — WS `trade/{id}`, бэкфил
  REST, тики в CSV с кольцом 14 дней. Модуль в `feeds/`, а НЕ в `core/`:
  `core` обязан парситься на py3.7 (там фид FXCM), код бота требует 3.10.
- Объём (`vol_base`/`vol_quote`) и **дельта** (агрессорный дисбаланс) в свече
  и на рэндж-барах. У FXCM объёма нет — свеча остаётся чистым OHLC.
- `core/volume_profile.py` — профиль объёма, логарифмическая сетка 0.05%,
  POC и область стоимости 70%, хранится вечно.
- `core/large_trades.py` — лента крупных сделок, порог = 95-й процентиль за
  скользящий час, гистограмма вместо сортировки.
- Префикс символов `provider:symbol`. Не косметика: у Lighter ЕСТЬ USDJPY,
  USDCAD, AUDUSD — те же пары, что у FXCM.
- Фронт: гистограмма объёма (цвет = знак дельты), легенда Vol/Δ, панель ленты.
- Глубина окна свечей по ТФ: M1 — неделя вместо 33 часов.

**Что сработало**

- Обкатка 14 ч: 0 ошибок фида, 0 обрывов связи с биржей, 21 754 события ленты.
- Диск втрое меньше прогноза: ~1 ГБ steady-state вместо 2.5-3 ГБ.
- Память хаба вышла на плато ~106 МБ — это окна 2000 баров, не утечка.
- Профиль пережил рестарт хаба: накопленное подхватилось из БД.

**Что не сработало сразу (7 багов)**

Четыре нашёл владелец на живом стенде — тесты бы их не поймали:

1. **Плоские рэндж-бары** (49% на MU). Гэп-путь закрывал бар при скачке > R
   даже если бар был из одного тика. У BTC не проявлялось: 470 тыс. сделок.
2. **История рэндж-баров не читалась** для крипты вовсе. `iter_ticks` брал цену
   из колонки `mid` (формат FXCM), а у Lighter колонка `price` — KeyError молча
   съедал каждый тик, бэкфил давал 0 баров при 12-мегабайтном архиве.
3. **Чужие бары при переключении** → `Cannot update oldest data`. `switchSymbol`
   не чистил `state.data`; на обычных ТФ незаметно (история приходит мгновенно),
   на рэндж-барах бэкфил идёт секунды.
4. **Дельта неочевидна**: цвет столбца её показывал, но это нигде не объяснялось.

Два бага лежали в master и к Фазе 3 отношения не имели:

5. **Нет `import json` в `core/db.py`** — хаб не пережил бы рестарт. Не стрелял,
   пока в `instruments` не появилась строка с непустым `meta`. Вынесен в master
   отдельным коммитом, хаб перезапущен.
6. **`instruments` отсекались фильтром по `requestId`** — селектор пар молча
   работал на захардкоженной HTML-разметке, precision оставался 5 для всех.
   В master НЕ выносился: фикс живёт в коммитах ветки и приедет вместе с
   вливанием (в отличие от п. 5, вынесенного отдельно).

7. **Порог ленты не соответствовал решению**: сравнение шло по окну ДО сделки,
   а в событие клался текущий порог — между ними кеш успевал обновиться.

**Уроки**

- Формат данных второго провайдера ломает молчаливые `except` — оба бага с
  чтением архива и с `instruments` глотались обработчиками ошибок.
- Неликвидные инструменты вскрывают то, что не видно на ликвидных: MU с 250
  сделками в сутки показал баг, невидимый на BTC.
- Скриншот находит то, чего не находят тесты: прозрачная панель и врущая
  заглушка проходили любые проверки кода.

---

## 2026-07-14 — Фаза 1: хранение свечей в SQLite (merge `534f82e`)

**Что сделано**

- `core/db.py` — SQLite: `candles` (PK `provider, symbol, tf, time`) + `instruments`,
  upsert / чтение / подрезка окна. `KEEP_BARS = 2000` баров на каждый (symbol, tf).
- `core/candles.py` — `CandleBuilder` (нарезка тиков по ТФ) + `aggregate_higher_tf`.
- `server.py` — `db_writer` пишет закрытые свечи в отдельном потоке через очередь;
  при старте история поднимается из БД, у FXCM запрашивается **только разрыв**.
- `H1` переведён в `DIRECT_LOAD_TF` (90 дней ≈ 1560 баров): агрегация из M1 давала
  167 баров, а индикаторам нужно 300–400 хвоста.
- `tests/test_phase1.py` — 17 тестов.

**Что сработало**

Приёмка пройдена. Прогон на порту 8767 (отдельный процесс, живой сервис не тронут):
первый старт с пустой БД — полная загрузка 30 дней; рестарт — `restored from SQLite`
+ дозагрузка разрыва. Ретеншен держит потолок: ни один ТФ не превысил 2000.
В проде после рестарта — то же самое, фронт работает.

**Что НЕ сработало — и чему это научило**

1. **Прод-python3.7 был собран без `_sqlite3`.** Тесты гонялись через `python3` (3.10),
   где sqlite3 есть, поэтому 17/17 «зелёных» ничего не доказывали. На проде
   `import sqlite3` падал.
   → Модуль собран вручную из `/root/Python-3.7.17/Modules/_sqlite/` и положен в
   `lib-dynload/`; интерпретатор не пересобирался, `forexconnect` цел.
   → **Правило: код для прода проверять командой `python3.7`, а не `python3`.**

2. **Рабочее дерево git оставалось на feature-ветке.** systemd запускает
   `/root/projects/terminal/server.py` **по пути** — какая ветка выкачена, тот код и
   исполняется. Автоперезапуск systemd подхватил код ветки, тот падал на `_sqlite3`,
   счётчик рестартов дошёл до 2529, порт 8765 умер, фронт отвалился.
   → **Правило: не оставлять дерево на ветке; для параллельной работы — `git worktree`.**

**Не сделано (осталось)**

Крон-остановка снята в тот же день — см. запись ниже.

**Данные**

`market.db` — боевой файл (в `.gitignore`), окно 2000 баров/ТФ, старое удаляется.
Вечный архив — по-прежнему тиковые `data/*.csv`: из них M1 восстановим точно,
из БД — уже нет. Секундные ТФ (S5–S30) выводятся только из живых тиков, при
рестарте их дыра = времени простоя (согласовано, не дефект).

---

## 2026-07-14 — снята крон-остановка сервиса `chart`

Строка `0 2 * * 2-6 systemctl stop chart` закомментирована в crontab.
`0 4 * * 1-5 systemctl start chart` **оставлена** как страховка на случай ночного
падения (watchdog появится только в Фазе 4).

Причина: после Фазы 1 история живёт в SQLite, и остановка сервера стала стоить
дорого — секундные ТФ (S5–S30) копятся только с живых тиков, из M1 не выводятся,
так что каждый простой = дыра в них. Крон давал такую дыру ежедневно, по два часа.

`CLAUDE.md` обновлён: раздела «Downtime window» как действующего больше нет.
Бэкап старого crontab лежал в скратчпаде сессии.

---

## 2026-07-14 — Фаза 2.1/2.2: сигналы выключены, данные заархивированы

**Что сделано** (ветка `phase-2-refactor`, слита в master, `c11ca46`)

- `SIGNALS_ENABLED = False` в `server.py`. Код движков не удалён — возврат = флаг в True.
- **Импорт** `pattern_memory` / `signal_tracker` тоже под флагом.
- `pre_session_brief.py`: техническая картина теперь из `market.db`, не из
  `market_memory.json`. `session_bias.json` больше не пишется.
- Крон: `asia_watcher` / `pos_watcher` закомментированы (читали замерший лог).
- `archive/old_data/` (249 МБ): `market_memory.json`, `signal_log.json`,
  `signal_stats.json`, `session_bias.json`, `briefing_context.json`, `History/`.
  `index_split.html` → `archive/`. `archive/` в `.gitignore`.

**Что сработало**

Рестарт чистый. RSS процесса **712 МБ → 137 МБ**. Ушли две полные перезаписи
(~100 МБ) каждые 240 секунд. Сервер к архивированным файлам не обращается:
mtime не двигался, `lsof` пуст, за 5+ минут ничего не пересоздалось.
Брифинг, молчавший с 09.07, ожил: ключ DeepSeek добавлен в `.env`, прогон
дал свежий `briefing.json` по всем 4 парам.

**Чему научило**

1. **Флаг обязан гасить и импорт, а не только вызов.** `pattern_memory` и
   `signal_tracker` читают свои JSON **на уровне модуля**: цена простого
   `import` — ~100 МБ RSS. Хуже: `pattern_memory` вешает `atexit`-сохранение,
   т.е. каждая остановка сервера = полная перезапись 73-МБ файла. Один `if`
   вокруг `process_tick` этого бы не убрал.

2. **Порядок 2.1 → 2.2 — не формальность, но и не единственная зависимость.**
   Кроме сервера те же JSON читал **крон** (`asia_watcher`, `pos_watcher`,
   `pre_session_brief`), чего в ROADMAP не было. Перед `mv` живого файла
   проверять не только `server.py`, но и `crontab -l`.

3. **Брифинг был привязан к побочному продукту сигналов.** Техническую картину
   он брал из `market_memory.json`. Выключи сигналы «в лоб» — и он бы молча
   описывал вчерашний рынок. Источник переведён на `market.db`: свечи живут
   независимо от сигнального контура.

**Известное, не чинилось**

RSS-фиды брифинга наполовину мертвы (Reuters закрыл публичные RSS): на прогоне
пришёл **1** релевантный заголовок. Календарь и техника на месте, новостная
часть худая. Не регресс Фазы 2 — так было и раньше.

Ретеншен показывает 2001–2009 баров вместо 2000: подрезка идёт раз в 200
записей (`DB_TRIM_EVERY`), а не после каждой свечи. Не дефект.

**Осталось в фазе**

2.3–2.6: разбор монолита (`core/` + `feeds/` + `hub.py`), контракт шины,
`precision` из `instruments` вместо хардкода на фронте.

---

## 2026-07-14 — кривые свечи H4/D1: две сетки в одной таблице (`45c75ac`)

Иван увидел на графике, что последняя H4-свеча «висит спайком». Оказалось —
три разных бага друг на друге.

**1. Незакрытая свеча не восстанавливалась после рестарта** (`65b856b`).
`load_history()` намеренно отбрасывает последний неполный бар (`.pop()`), чтобы
он не попал в БД как закрытый, но `current_candle` при этом никто не заполнял —
и `process_tick` строил свечу заново от первого тика: `open` = цена в момент
рестарта. Данные не терялись, просто не использовались.
→ `seed_current_candles()` собирает бар текущего бакета из M1, уже лежащих
в памяти (SQLite + разрыв от брокера).

**2. Две сетки H4/D1 — это и был «кривой» график** (`a1434d3`).
FXCM отдаёт H4 по **01/05/09/13/17/21 UTC**, D1 — по **21:00 UTC**: сетка
привязана к торговому дню (закрытие Нью-Йорка 17:00 UTC), а не к полуночи.
А `process_tick` резал ВСЕ ТФ как `ts // sec`, т.е. по UTC → рождалась вторая
сетка (00/04/08/12/16/20), и наши бары вставлялись ВНУТРЬ брокерских. В БД
шаги между H4-барами шли `+4, +3, +1, +4` — график ломался.
→ `state["tf_offset"]` + `detect_tf_offsets()` (учит смещение из последнего бара
брокера) + `_bucket_of()` — единственная точка расчёта бакета.
→ Данные: удалены 2961 бар H4/D1 старых сеток, сервер перезалил их у FXCM.

**3. Таймер ТФ** показывал `238:47` на H4 (всегда MM:SS). → ЧЧ:ММ:СС от часа.

**Чему научило**

1. **Не чинить данные по недоказанной теории.** Я решил, что сетка сдвинута
   из-за `to_timestamp()`: сервер стоит в UTC+5, а `.timestamp()` трактует naive
   datetime как локальное время. Уже собирался чистить БД — Иван спросил про
   таймзону, я пошёл **спрашивать брокера живым запросом**, и версия рассыпалась:
   FXCM отдаёт `numpy.datetime64` (правильная ветка, без сдвига), а сетка такая
   у него самого. Почисти я БД тогда — получил бы тот же мусор через час.

2. **Смещение сетки учить из данных, а не константой.** У D1 оно плавает:
   21:00 летом, 22:00 зимой (переходы 02.11.2025 и 08.03.2026 — видны в БД).
   Хардкод `21ч` сгнил бы при следующем переводе часов.

3. **Проверять не логи, а то, что сервер реально отдаёт в WS.** После первого
   фикса логи говорили `Seed ... restored for 5 TFs`, числа O/H/L совпадали с
   эталоном — и я отрапортовал «работает». Но не посмотрел **время** баров, где
   и сидел баг №2. Эталон надо сверять целиком, включая стык с историей.

**Мина на будущее (не выстрелила, но заряжена)**

`to_timestamp()`, ветка `isinstance(dt, datetime)`: `.timestamp()` на naive-времени
даёт сдвиг на таймзону сервера (UTC+5). FXCM отдаёт `datetime64` → не стреляет.
При подключении Lighter (Фаза 3) обезвредить.

---

## 2026-07-15 — живая свеча отдавалась не сразу, а со следующим тиком (`854f652`)

Иван: «при смене инструмента активная свеча появляется примерно через секунду».

**Причина:** на `set_tf` сервер слал только `tf_data[tf]` — ЗАКРЫТЫЕ свечи.
`current_candle` уходила лишь со следующим сообщением `update`, т.е. с ближайшим
тиком. Замер на живом сервере: история приходит за **0.02 с**, активная свеча —
через **0.45–5.81 с** (на M1 однажды 5.81 с — просто не было тика).
→ Теперь `current_candle` уходит сразу за историей тем же `update` (тот же
`requestId`, после `history` — порядок сообщений в WS гарантирован, гонки нет).
Фронт не менялся. После правки: **0.02 с на всех 9 ТФ**.

**Грабли, не связанные с задачей**

`server.py` — файл со **смешанными окончаниями строк** (1319 строк CRLF из ~1330).
Правка через `python3 -c "open(...).write(...)"` молча нормализовала их в LF, и
`git diff` показал **2654 изменённые строки** вместо 16. Поймал до коммита.
→ Правь `server.py` только Edit-ом (точное совпадение строки), а не перезаписью
файла целиком. `git diff --stat --ignore-all-space` показывает правду.

---

## 2026-07-15 — разрыв после рестарта: `open` восстановленной свечи (`50e63ac`)

Иван: «первая M1 после рестарта начинается с разрывом; open должен быть = prev.close всегда».

**Причина:** `open` определялся ДВУМЯ способами. В живом потоке (`process_tick`,
ветка `sec >= 60`) `open = prev_close` — свечи встык. А `seed_current_candles()`
после рестарта брал `open` из первой минутки бакета → скачок.
→ Теперь `open` = close последней ЗАКРЫТОЙ свечи того же ТФ. Строго раньше
`bucket`: в `tf_data[tf]` может лежать незакрытый бар текущего бакета от брокера
(H4/D1 грузятся напрямую). `high`/`low` включают `open`, как в живом потоке.

**Дыра на M1 — из-за неё жалоба и возникла.** Для M1 бакет = ТЕКУЩАЯ минута, а её
закрытой минутки в истории по определению нет → `bars` всегда пуст → `continue` →
ТФ пропускался целиком, и первый тик открывал свечу от своей цены. M5+ спасались
тем, что в их бакет уже попали закрытые минутки. Первая версия правки этого бы не
поймала — нашёл, только прогнав результат на живых данных.
→ При пустом `bars`, но известном `prev_close` свеча создаётся сразу от него
(вся в одной точке), первый тик её дополняет. В логах: `restored for 7 TFs`
вместо 6 — добавился M1.

**Секундные ТФ (S5–S30) не трогали:** в живом потоке открываются от цены тика,
гэп там — норма (согласовано с владельцем). `seed` их пропускает.

Проверено на проде через WS: M1/M5/M15/H1/H4 × 2 пары — разрыв 0.0 пипс.

**Про cheap_agent** (Иван просил делегировать): логику выдал ВЕРНУЮ, но код бы не
запустился — выдумал `candles_state`, `state['data']`, `history_data`, которых в
проекте нет, хотя `server.py` был передан ему целиком. Дыру на M1 тоже не увидел.
Схема «спека моя → код его → проверка моя» рабочая, но принимать вывод на веру
нельзя.

---

## 2026-07-15 — Фаза 2.3: фундамент разбора монолита — `candles` + шина (`885a358`, `0a25b6f`)

Начало распила `server.py` на процессы. Причина не косметическая: `forexconnect`
требует Python **3.7**, SDK Lighter — **3.8+**; в одном процессе они несовместимы,
значит фиды обязаны стать отдельными процессами, а между ними нужна шина.

**1. `core/candles.py` устарел, ни разу не исполнившись** (`885a358`).

Модуль написан в Фазе 1, но `server.py` его так и не использовал — и он успел
протухнуть: резал бакет как `ts // sec * sec`, т.е. по UTC-полуночи. Ровно этот
баг мы чинили вчера в `server.py` (`a1434d3`): у FXCM H4 идёт по 01/05/09/13/17/21
UTC, D1 — по 21:00 (торговый день). Подключи хаб к `CandleBuilder` как есть —
кривые H4/D1 вернулись бы, но уже в новом коде.

→ `_bucket_of()` + `tf_offsets` — единственная точка расчёта бакета.
→ `detect_offsets()` учит смещение **из данных**: у D1 оно плавает (21:00 летом,
   22:00 зимой), константа сгнила бы дважды в год.
→ `seed_current()` — незакрытая свеча после рестарта (перенос `seed_current_candles`).
→ Время бара стало `int`: в проде `ts = time.time()` — **float**, и он утекал в БД
   (SQLite молча схлопывала в INTEGER, поэтому никто не замечал).

**2. `core/bus.py` — шина фиды → хаб** (`0a25b6f`), WS на 127.0.0.1:8766.
Контракт `tick` / `instruments`, время **только в секундах**: `validate()` ловит
миллисекунды (`ts > 1e11`) — Lighter отдаёт мс, и это единственное место, где их
можно перехватить до порчи свечей.

**Чему научило**

1. **Зелёный тест, который ничего не проверял.** Реплей тиков брал «самый свежий»
   CSV — а это сегодняшний файл на 33 тика за 42 секунды. По M3/M5/H1/H4/D1 не
   закрылось НИ ОДНОЙ свечи: те самые ТФ, ради которых вся правка с сеткой, не
   проверялись вовсе, а прогон был зелёный. → Берём самый КРУПНЫЙ файл (94 606
   тиков, 24 ч) плюс тест-страховка `test_replay_is_not_vacuous`, который падает,
   если реплей опять выродится.

2. **Баг, невидимый на 3.10 и смертельный на 3.7.** `asyncio.Queue()`, созданная
   в `__init__`, на py3.7 привязывается к тому event loop, что был при создании,
   а `asyncio.run()` заводит новый → первый же `await get()` на пустой очереди
   (штатное состояние шины между тиками) падает с `got Future attached to a
   different loop`. На 3.10 — тишина. Тесты на «своём» питоне ничего не сказали бы
   про фид FXCM. → Всё, что живёт на обоих питонах, гоняем на **обоих**.

3. **Потеря тика на обрыве.** `_pump` висит на `queue.get()`, об оборванном сокете
   не знает; пришедший тик снимался с очереди, `ws.send()` падал — тик исчезал.
   → Слот `_pending`: сообщение подтверждается только успешной отправкой.

**Про cheap_agent** (`use_pro=true`): оба файла писал он по моей спеке. Логика
верная, API не выдумывал (в отличие от прошлого раза) — помогло то, что в спеку
я явно вписал список запрещённых вызовов. Но оба бага выше — его: он не знал ни
про разницу websockets 11/16, ни про то, что на 3.7 очередь привязывается к loop.
Схема «спека моя → код его → проверка моя» держится, но проверка обязана быть
на реальных данных и на реальном питоне.

**Осталось в фазе**

`retention.json`, `feeds/fxcm_feed.py`, `hub.py` (порт 8767 параллельно живому
`server.py`), `precision` из `instruments` на фронте, миграция и снос монолита.

---

## 2026-07-15 — Фаза 2.3: хаб и фид — монолит разобран (`3221bf6`, `03a7373`)

Структура из ROADMAP собрана: `core/` (шина, свечи, БД) + `feeds/fxcm_feed.py`
(py3.7, только брокер) + `hub.py` (py3.10, всё остальное) + `retention.json`.
Живой `server.py` не тронут, всё лежит в ветке `phase-2-refactor`.

**Решения, которых не было в ROADMAP**

1. **Хаб пишет в СВОЮ базу** (`market_hub.db`). Пока он крутится параллельно на
   8767, писать в боевую `market.db` под тем же `provider="fxcm"` нельзя: два
   писателя в одни ключи `(provider, symbol, tf, time)` плюс независимая подрезка
   окна — гарантированная порча живых данных. Сверка идёт между базами.

2. **Контракт шины дополнен типом `candles`.** В ROADMAP его не было, и это дыра
   в архитектуре: хаб к брокеру не ходит, а историю за время простоя может
   догрузить только фид (в монолите — `load_history`). Без `candles` хаб после
   каждого рестарта оставался бы с дырой в свечах.

**Три бага cheap_agent, пойманных на ревью кода**

1. **Тик уходил в шину только при смене цены.** `send` был засунут под
   `should_emit`, который в монолите гейтит ТОЛЬКО запись в CSV: `process_tick`
   зовётся на КАЖДОМ тике. В мёртвом рынке цена стоит минутами → хаб не получил бы
   тика и не открыл бы новую свечу. В истории появились бы пропущенные минуты, и
   заметили бы это в лучшем случае через неделю.

2. **`fx.get_history(symbol, "m1", None, None, 10000)`** — такой формы вызова в
   монолите нет, он всюду ходит `get_history(symbol, tf, start, end)` с датами.
   Классика: правдоподобный API, которого не существует.

3. **Пачка истории не влезала в сообщение.** 10 000 минуток = ~750 КБ JSON при
   лимите входящего сообщения `websockets` в 1 МБ. Форекс прошёл бы впритык, а
   крипта с объёмами — нет: хаб рвал бы соединение на первой же догрузке. Пачки
   режутся по 2000 баров, лимит шины поднят до 8 МБ.

**Чему научило**

*Тест, который «зелёный», потому что ничего не делал* — второй раз за фазу. Первый
прогон хаба принял 6000 тиков и сверил свечи с эталоном… по одному ТФ, потому что
остальные не успели закрыться. Пока не посмотришь на ЧИСЛА (сколько баров реально
закрылось), «OK» в юнит-тесте не значит ничего.

*Дырка в `core/db.py` с Фазы 1*: `upsert_instrument` писал БЕЗ коммита
(`conn.execute` без `with conn:`, в отличие от `upsert_candle`). Метаданные
инструментов молча умирали вместе с процессом. Всплыло только сейчас, когда
появился первый настоящий потребитель.

**Что осталось до переключения фронта**

- Фид **ни разу не запускался живьём**: он открывает свою сессию к FXCM, а тем же
  логином уже подключён боевой `server.py` — второй логин может выбить первую
  сессию. Запуск только по согласованию с владельцем.
- Хаб v1 не умеет: алерты, брифинг, панель analysis. Портировать до переключения
  UI, иначе фронт потеряет функции.
- 2.5: `precision` из `instruments` (у USD/JPY 3 знака, а во фронте хардкод 5),
  символы вида `fxcm:EUR/USD`.

---

## 2026-07-15 — переключение прода на хаб+фид (боевой контур)

Живой контур переведён с монолита на новую архитектуру. Порядок:
1. `systemctl stop chart` — освободил 8765, `server.py` штатно закрыл сессию FXCM
   (иначе фид не залогинился бы — конфликт сессии одним аккаунтом).
2. Хаб на **8765** (не 8767!) и на **боевой** `market.db` (абсолютный путь в
   `retention.json`): фронт стучится `ws://89.107.10.204:8765` жёстко в HTML, так
   что сел на его порт и его базу — фронт не трогали вообще.
3. Фид `python3.7 -m feeds.fxcm_feed`, env из боевого `.env`.

**Проверено живьём (прямыми WS-запросами, не по логам):** живая свеча растёт в
реальном времени; стык `open == last_close`, разрыв **0.0 пипс**; `instruments`
приехали и `USD/JPY` отдаёт **price_decimals=3** (было хардкодных 5); свежайший M1
отстаёт на минуту (норма), S5 растут с живых тиков; в `market.db` пишется.

**Первый живой запуск фида** прошёл без инцидента: вторая сессия FXCM тем же
аккаунтом БОЕВУЮ не выбила (боялись этого — оказалось, `stop chart` перед стартом
фида снял риск, сессии не пересекаются во времени).

**Защита от возврата монолита (откатываемо):**
- крон `0 4 * * 1-5 systemctl start chart` **закомментирован** (иначе в 04:00
  будни server.py перехватил бы 8765);
- `systemctl disable chart` (иначе поднялся бы при ребуте).
- Бэкапы: `scratchpad/market.db.backup-preswitch`, `scratchpad/crontab.backup`.

**МИНА: у хаба и фида НЕТ автозапуска.** Упадут или ребут — не поднимутся сами.
Руками из `terminal-phase2`: `python3.10 hub.py` и
`set -a; . /root/projects/terminal/.env; set +a; python3.7 -m feeds.fxcm_feed`.
Откат к монолиту: `systemctl enable --now chart` (сперва погасить хаб — конфликт 8765).

---

## 2026-07-15 — systemd-юниты для хаба и фида (снят «прод на ручнике»)

После переключения прода хаб/фид жили ручными фоновыми процессами — без
автозапуска. Заведены сервисы `chart-hub` (py3.10, hub.py) и `chart-feed` (py3.7,
feeds.fxcm_feed), оба `Restart=always`, `enabled`, `WorkingDirectory` = worktree
`terminal-phase2`. Фиду окружение (FXCM_*) даёт `EnvironmentFile=` боевого `.env`.
`chart-feed` идёт `After=chart-hub`, но жёсткой зависимости нет — фид сам
переподключается к шине с backoff.

Замена ручных процессов на сервисы — атомарно: kill обоих → 8765/8766 свободны →
`start chart-hub` → `start chart-feed`. Простой контура ~5 сек.

**Проверено живьём:** `Restart=always` реально работает — `kill -9` фида, systemd
поднял новый за ~секунды, тот перелогинился к FXCM и вернулся на шину. Данные
текут, живая свеча идёт.

Копии юнитов в репозитории: `deploy/chart-hub.service`, `deploy/chart-feed.service`
(боевые — в `/etc/systemd/system/`).

Итого сервисы прода: `chart-hub` + `chart-feed` (новьё) + `chart-frontend` (:8082,
без изменений). `chart` (монолит) — stopped + disabled, откат к нему возможен.

---

## 2026-07-15 — алерты перенесены в хаб (пункт 2 пост-переключения)

Хаб v1 игнорировал add/update/remove_alert — фронт слал их впустую. Перенёс из
server.py: срабатывание по ПЕРЕСЕЧЕНИЮ уровня (в любую сторону) в `_handle_tick`
(для этого хаб держит `last_price[symbol]`), `alert_created` с серверным id
(фронт подставляет его вместо `id=null`), событие `alert` — всем клиентам.
Контракт полей сверен по `index.html`/`js`.

Осознанное отличие: `update_alert` сбрасывает `triggered` — сдвинутый на новое
место сработавший алерт снова заряжен. В монолите он оставался немым (баг).

**Проверено на проде:** `alert_created id=3 price=1.2` + `remove_alert` приняты
через боевой WS. Срабатывание по пересечению доказано тестом (реальный WS, тик
через уровень → `alert`) — на живом проде его не поймал, т.к. рынок стоял (тиков
между минутами почти нет). Тесты: 7 шт., плюс hub e2e (4) без регрессий.

Грабли: агент Explore для сбора протокола алертов упал — Explore ушёл на
несуществующую модель `deepseek-v4-flash`. Протокол собрал сам (он компактный).

Выкат: `systemctl restart chart-hub` (~5 c простоя), фид переподключился к шине сам.

---

## 2026-07-15 — precision из instruments на фронте (пункт 3, Фаза 2.5-частично)

`index.html` (боевой, ветка master) читал precision захардкоженным 5 для всех
пар — у USD/JPY их 3, цена рисовалась с двумя лишними нулями. Хаб уже слал
instruments с price_decimals (Фаза 2.3), фронт их игнорировал.

4 аддитивные правки: карта `instrumentPrecision` + `applyPriceFormat(id)`, ветка
`instruments` в handleMessage, применение при создании серии и при switchSymbol.
Дефолт 5 сохранён, если instruments ещё не пришли.

Проверено: `node --check` валиден; логика прогнана на реальном формате instruments
прода → USD/JPY {precision:3, minMove:0.001}, прочие 5. Браузером не гонял:
puppeteer-core в этом дереве не стоит, а ставить ради одной проверки — избыточно;
логика фронта изолируется и проверяется на node без браузера.

Фронт статический — вступает в силу по F5, `chart-frontend` не перезапускали.
Бэкап: `scratchpad/index.html.backup-precision`.

Остался пункт 4 — брифинг в хаб (панель на фронте, хаб пока не шлёт briefing).

---

## 2026-07-15 — брифинг в хабе (пункт 4) + все 4 пост-переключения закрыты

Панель Briefing не получала данных — хаб не слал briefing. Перенёс из server.py:
поток `briefing_watcher` поллит `briefing.json` по mtime раз в 5 с и рассылает
всем; новый клиент получает кэш при коннекте. Путь — `briefing_file` в конфиге,
АБСОЛЮТНЫЙ (файл в боевом дереве, хаб в worktree). Рассылка из потока-демона →
`_broadcast_threadsafe` (call_soon_threadsafe). Крон не трогали.
**Проверено на проде:** новый клиент получил briefing session=ny.

**Итог четырёх пунктов пост-переключения (все на проде):**
1. systemd-юниты chart-hub/chart-feed (Restart=always, enabled) — снят «ручник».
2. Алерты — перенос, срабатывание по пересечению, контракт сверен.
3. precision из instruments на фронте — USD/JPY 3 знака (живьём: close=162.203).
4. Брифинг — poll + broadcast.

Финальный контур прода: `chart-hub` (:8765) + `chart-feed` (:8766) +
`chart-frontend` (:8082). `chart` (монолит) — inactive/disabled, откат возможен.
Полный WS-контур проверен одним заходом: history/update/briefing идут, свеча живая.

**Что монолит умел, а хаб пока НЕ портирован (сигнальный контур ВЫКЛЮЧЕН с Фазы
2.1, поэтому это не регресс):** analysis-панель (send_analysis), сигналы
(send_signal / get_signals / signals_history). Фронт эти сообщения обрабатывает,
но их источник — выключенные движки. Портировать только если вернём сигналы.

**Осталось в Фазе 2:** символы `fxcm:EUR/USD` вместо голых (2.5), снос server.py
в archive/ после периода наблюдения, разбор оставшихся хвостов монолита.

---

## 2026-07-15 — Фаза 2.5: селектор пар из instruments + структурный фикс

**Селектор пар из instruments** (фронт): был захардкожен в HTML-разметке, теперь
строится из WS-сообщения instruments (populateSymbolSelect: сохраняет выбор, не
дёргается при совпадении набора). onopen шлёт get_instruments — хаб отдаёт их при
коннекте только если уже получил от фида, а браузер может подключиться раньше.

**Префикс provider:symbol (fxcm:EUR/USD) СОЗНАТЕЛЬНО отложен до Фазы 3.** Он нужен
только при втором провайдере (коллизия BTC), а сейчас лишь сломал бы localStorage
и UI (currentSymbol в 22 местах). Под Lighter (другой формат символов) его всё
равно переделывать. Отмечено в ROADMAP 2.5.

**Структурный фикс (всплыл при проверке):** get_instruments на проде отдавал
ПУСТОЙ список. Причина — фид шлёт instruments один раз при логине, а мои четыре
рестарта chart-hub (под алерты/брифинг) их теряли: _instruments в памяти
обнулялся, фид повторно не слал. Тот же класс дыры, что была с историей свечей.
→ core/db.load_instruments + Hub.restore восстанавливает их из БД (туда они
пишутся в _handle_instruments). Рестарт хаба больше не зависит от рестарта фида.
Проверено на проде: `[restore] fxcm — инструменты: AUD/USD, EUR/USD, USD/CAD,
USD/JPY` при старте хаба, без рестарта фида.

**Заодно:** в юниты добавлен `Environment=PYTHONUNBUFFERED=1` — без него print из
хаба/фида не долетал до journald, диагностика шла вслепую. Копии в deploy/ обновлены.

Тесты: instruments 3 (вкл. рестарт-восстановление), полный набор без регрессий
(phase1 17 на py3.7, hub 4, alerts 7, briefing 3).

---

## 2026-07-15 — тулбар графика: иконки + группы, Layout в «⋯»

Тулбар был перегружен (9 текстовых кнопок), после Lighter стало бы тесно.
Переработан по согласованному макету (артефакт показывался владельцу):

- Инструменты рисования — сегментная группа иконок; Позиция/Индикаторы/Sync —
  иконки с tooltip (inline SVG). Сигналы/Brief — текст с иконкой (входы в панели).
- Меню «⋯»: Удалить всё, Сбросить масштаб, Save/Load разметки.
- Плавающая кнопка Layout убрана (налезала на новый тулбар и кнопки масштаба),
  Save/Load переехали в «⋯».

**Важно для будущих правок тулбара:** подсветка активного инструмента теперь
через КЛАССЫ (.on / .on-long / .on-short в CSS index.html), а НЕ через inline
`btn.style.background`. Иначе синяя заливка ломает иконочные кнопки. Подсветка
живёт в ДВУХ местах: ui-tools.js (панель 1) и обёртка setTool в index.html
(панель 2 — там и только там подсвечивается вторая панель).

**Мины при переработке** (обрывали бы JS, все обезврежены): clearDrawingsBtn и
layoutBtn привязывались по id в JS, а кнопки убраны → getElementById(...).onclick
на null; мобильная ⚙️ Layout открывала удалённый #layoutDropdown → на «⋯».

Проверка: node --check обоих файлов, id-целостность, клики — владельцем в браузере.
Тёмная тема — отдельной задачей (сейчас цвета захардкожены, нет CSS-переменных).
Бэкап: scratchpad/index.html.backup-toolbar.

---

## 2026-07-15 — тёмная тема (переключатель в меню настроек графика)

Цвета вынесены в CSS-переменные (:root светлая = дефолт, [data-theme=dark]).
Переключатель ☀/☾ — в #contextMenu (правый клик по графику → Настройки графика),
не в тулбаре (по просьбе владельца). Персист в layout.theme (localStorage).
График LWC красится через applyOptions (не читает CSS-переменные). Панели
сигналов/брифинга — тёмные оверлеи в обеих темах. Бэкап index.html.backup-theme.

---

## 2026-07-17 — Тиковый архив объединён (подготовка к рэндж-барам)

**Проблема.** Переключение прода на хаб+фид (15.07) раздвоило вечный тиковый архив:
`terminal/data/` (монолит, до 15.07 05:03 UTC) и `terminal-phase2/data/` (фид, после).
`market.db` при миграции симлинкнули, а `data/` — нет. Любой потребитель полной
тиковой истории (рэндж-бары, бэктесты) получал бы дыру на стыке.

**Что сделано**

- Бэкап всех 12 затрагиваемых файлов → `archive/tick_merge_backup_20260717/`.
- Файлы за **15.07** оказались взаимодополняющими (монолит 00:00→05:03:36,
  фид 05:04:40→24:00, пересечения нет) — хвосты фида дописаны к мастерским
  (`tail -n +2`), суммы строк сошлись по всем 4 парам, монотонность проверена.
- Файлы фида за **14.07** — 40-секундный тестовый огрызок (14–34 строки),
  полностью покрытый полными файлами монолита → отброшены (бэкап есть).
- Уникальные файлы (16–17.07) перенесены `mv` в `terminal/data/`.
- `terminal-phase2/data` → симлинк на `/root/projects/terminal/data`.

**Без рестарта фида.** tick_writer держит открытые хэндлы и переоткрывает файлы
только при смене даты (00:00 UTC): `mv` в пределах одной ФС сохраняет inode, фид
продолжил писать в перемещённый файл. Проверено: строки в EURUSD_20260717.csv
растут после переноса, `dropped: 0`, оба сервиса active.

**Урок.** DATA_DIR в фиде относительный (`"data"`) — при переезде WorkingDirectory
такие пути тихо плодят вторую копию данных. При мёрже phase-2 в master симлинк
разрешится сам; альтернатива — абсолютный путь в конфиге, как у `db_path`.

---

## 2026-07-17 — merge: phase-2-refactor → master (`b6adfde`)

Цели ветки закрыты (Фаза 2 целиком), расхождение оказалось чистым: фронт коммитился
только в master, бэкенд — только в phase-2; единственный конфликт — LOG.md, записи
склеены по хронологии. Полный прогон тестов на смерженном master: phase1 17 (py3.7),
candles, bus 13 (py3.7), hub 4, alerts 7, briefing 3, instruments 3 (py3.10) — все OK.

Сервисы не трогались: юниты по-прежнему смотрят в worktree `terminal-phase2`,
ветка там подтянута fast-forward к мержу — деревья идентичны. Осталось (на выходных,
форекс закрыт): переключить WorkingDirectory юнитов chart-hub/chart-feed на
`/root/projects/terminal`, рестартнуть, снять worktree.

---

## 2026-07-17 — Рэндж-бары, шаг 2: `core/range_bars.py` (ветка `range-bars`)

**Что сделано**

- `RangeBarBuilder` — тики → бары фиксированного диапазона. Решения зафиксированы
  в докстринге модуля: закрытие «на пробое» (тик-пробойщик включается, диапазон
  >= R), open = close предыдущего, гэп = один честный широкий бар (без фантомной
  лесенки TradingView), время бара уникально-возрастающее (+1с при коллизии —
  требование LightweightCharts). range_size — в абсолютных единицах цены,
  пересчёт из пипсов — забота хаба (у него instruments.price_decimals).
- `iter_ticks` + `backfill` — история из тикового архива `data/*.csv`
  (рэндж-бары честно строятся только из тиков; объединение архива — см. выше).
- `tests/test_range_bars.py` — 10 тестов (8 синтетика + 2 на реальном дне
  EUR/USD), зелёные на py3.7 и py3.10.

**Замеры на реальном архиве EUR/USD (для дизайна шага 3):**
R=5 пипсов ≈ 67 баров/день (2000 баров ≈ 30 дней), R=10 ≈ 16/день, R=20 ≈ 4/день.
Полный бэкфил архива ~10 с → в хабе делать лениво в фоне + кэш по (symbol, R),
закрытые бары персистить в candles (tf="R:5") — рестарт тогда мгновенный.

Следующий шаг (3): интеграция в hub.py — tf вида "R:<pips>" в set_tf, живой
ingest из шины, персист/restore.

---

## 2026-07-17 — Рэндж-бары, шаг 3: интеграция в хаб (ветка `range-bars`)

**Что сделано**

- `hub.py`: `set_tf` с tf вида `"R:<пипсы>"` (в т.ч. дробные, `R:2.5`) — ленивый
  бэкфил из тикового архива в отдельном потоке, кэш билдеров по (provider,
  symbol, pips), живой ingest из шины, рассылка по контракту свечных ТФ
  (history → update, requestId сохраняется — фронт не отличает рэндж от свечей).
- Пипс из `instruments.price_decimals` (EUR/USD 0.0001, USD/JPY 0.01), дефолт
  0.0001. `data_dir` в retention.json (дефолт `"data"`).
- Дыра на время бэкфила закрыта буфером: тики шины копятся в pending и
  вливаются после архива с дедупом по ts (CSV пишется тем же потоком с лагом
  <1 с, хвосты пересекаются).
- Отдельная рассылка финала закрытого бара: тик-пробойщик мутирует
  закрывающийся бар и сразу открывает новый — без явной отправки закрытого
  клиент никогда не увидел бы его финальную форму (broadcast шлёт current).
- Персиста в candles СОЗНАТЕЛЬНО нет: произвольные R замусорили бы таблицу,
  а источник истины один — тиковый архив; после рестарта история честно
  перестраивается из него (≈10 с на пару, один раз на (symbol, R)).
- `tests/test_range_hub.py` — 3 теста: parse/пипс + сквозной (синтетический
  архив, ожидания посчитаны руками: бэкфил → history/update → живое закрытие
  тиком шины → кэш при повторном set_tf).

Полная регрессия: 80 тестов (9 файлов, py3.7+py3.10) — все OK.

Следующий шаг (4): фронт — опция Range в селекторе ТФ, гварды tfSeconds
(таймер → прогресс диапазона, crosshair-sync), сохранение в layout.

---

## 2026-07-17 — Рэндж-бары, шаг 4: фронт (ветка `range-bars`)

**Что сделано**

- Селектор ТФ: пресеты R5/R10/R20 + пункт «R…» (prompt, произвольное значение,
  в т.ч. дробное; опция создаётся динамически и переживает перезагрузку через
  layout). tf хранится строкой "R:<пипсы>" — StorageLayer не менялся.
- `setTF`: для рэндж-ТФ `tfSeconds = null`, `rangePips` — распарсенный диапазон.
  Вся временная логика проверяет tfSeconds: таймер, сигнальные маркеры (на
  рэндж-ТФ их нет — снапить не к чему), crosshair-sync (уже был под гвардом),
  secondsVisible = true.
- Таймер обратного отсчёта на рэндж-ТФ показывает прогресс диапазона «3.2/5»
  (пипс из instruments.precision, дефолт 5 знаков).
- `TimeMapper.bucketTime`: при tfSeconds=null время рисунка не снапится к
  бакету (раньше вернул бы null в LWC).
- Хардкод адреса хаба вынесен в `WS_URL` с переопределением `?ws=` — нужно для
  headless-теста против локального хаба, полезно вообще (хвост из ROADMAP).

**Проверка (headless chromium + puppeteer, тестовый хаб на 8767, temp-БД,
реальный тиковый архив):** пресет R:10 — 1375 баров, min диапазон ровно 10
пипсов, времена строго возрастают, open = prev close по всей истории, метка
«9.2/10»; кастом через prompt — «R:7» в селекторе, 2000+1 баров по 7 пипсов;
откат на M1 чистый. **Ноль JS-ошибок за весь прогон.** Скриншот глазами:
график рендерится, бары равного диапазона.

Осталось до мержа: показать вживую владельцу (нужен выкат хаба ветки — рестарт
chart-hub, согласовать по времени), решить судьбу «прокрутки влево» (окно 2000
баров, как у свечных).

---

## 2026-07-17 — Тип отрисовки «бары» в дополнение к свечам (ветка `range-bars`)

**Что сделано (чистый фронт, бэкенд не трогался)**

- Тумблер «🕯 Свечи / 📊 Бары» в контекстном меню графика (там же, где тема),
  действует на панель, по которой меню открыто (chartMenuPane). Тип — per-pane,
  хранится в `layout.pane<N>.chartType`, переживает перезагрузку.
- `makeSeries(chart, type)` создаёт CandlestickSeries либо BarSeries;
  `setChartType(paneId, type)` меняет тип уже живого графика: LWC не позволяет
  сменить тип серии на месте, поэтому старая серия сносится и создаётся новая,
  на неё переносятся данные, precision, цвета, привязка индикатор-менеджера
  и drawing-engine, а плагин сигнальных маркеров пересоздаётся (жил на старой
  серии). Индикаторы держат СВОИ серии — их смена типа не осиротила.
- `applyColors` разветвлён по типу: у BarSeries нет фитилей/границ, только
  up/down. Дубль applyColors/resetColors удалён из context-menu.js (версия из
  index.html знает про тип — порядок загрузки скриптов больше ничего не решает).

**Проверка (headless chromium против прод-хаба 8765, локальный фронт):**
свечи→бары→свечи, тип серии реально Candlestick↔Bar, 2001 бар переносится,
SMA-индикатор жив после переключения, цвет применяется, тип восстановлен из
layout после reload. Ноль JS-ошибок. Скриншот: бары рисуются палочками с
засечками, SMA поверх.

---

## 2026-07-17 — Фикс: не выйти из фулскрина после перезагрузки (ветка `range-bars`)

**Симптом (давняя регрессия, ещё со времён GPT-фронта):** если перезагрузить
страницу, когда одна панель развёрнута на весь экран, вернуться в сплит
невозможно — кнопка ⛶ не реагирует, ошибок в консоли нет.

**Причина.** Два несовместимых механизма скрытия колонки:
- восстановление при загрузке прятало колонку КЛАССОМ `.hidden`
  (`display:none !important`);
- `toggleFull` (выход из фулскрина) снимает скрытие ИНЛАЙНОВЫМ
  `style.display = ""`.

Инлайновый `""` не перебивает `!important` из класса — класс оставался,
колонка не возвращалась. Плюс восстановление не выставляло `flex:1 1 100%`
видимой колонке (toggleFull выставляет).

**Фикс.** Восстановление split приведено к тому же механизму, что и toggleFull:
инлайновый `style.display="none"` + `flex` видимой колонки, без класса
`.hidden`. Теперь вход/выход симметричны.

**Проверка (headless):** фулскрин L → reload → выход в сплит; то же для R.
Все переходы корректны (split/видимость колонок сходятся), ноль ошибок.

---

## 2026-07-17 — Рэндж-бары: фикс раздутых баров на гэпах (ветка `range-bars`)

**Симптом (нашёл владелец на графике).** При R=10 отдельные бары были размахом
30-55 пипсов — в 3-5 раз больше нормы. Не иллюзия хвостов: по данным реальный
размах (high-low) у них 54.8п при пороге 10.

**Причина.** Гэп на закрытии рынка (выходные, новостной скачок): цена прыгает
через порог ОДНИМ тиком на 50+ пипсов. Старая логика: (1) новый бар подтягивался
к close предыдущего (open=prev_close), (2) гэп-тик вливался в текущий бар как
обычное расширение high/low. Оба всасывали скачок в один бар, раздувая его.

**Решение (по требованию владельца: рэндж-бары могут иметь гэпы, не подтягивать
open к prev close).**
- `_open_bar`: новый бар открывается от ЦЕНЫ ТИКА-пробойщика, а не от prev_close.
- `ingest`: если тик двигает цену от текущего close бара БОЛЬШЕ чем на R
  (`|price - close| > R`) — это гэп: текущий бар закрывается КАК ЕСТЬ (до скачка),
  новый открывается от цены тика. Разрыв остаётся видимым гэпом между барами.
  Порог строгий (> R+eps), чтобы ровный пробой в R гэпом не считался.

**Результат на реальных тиках EUR/USD R:10:** макс размах 54.8п → 15.9п, баров
>12п: 26 → 12 (это уже честные быстрые движения в пределах R, не гэпы), медиана
10.1п без изменений. Нормальный пробой не задет.

Тесты переписаны под новую семантику (open от тика, гэпы разрешены,
размах < 20п инвариантом). Полная регрессия 81 тест — зелёная.

---

## 2026-07-17 — Рэндж-бары: единица в ПОИНТАХ (как TradingView) (ветка `range-bars`)

**Зачем.** Наш R10 был = 10 пипсов, а TradingView и все остальные меряют
рэндж в поинтах (пипеттах, минимальный тик). Их «10R» = 10 поинтов = 1 пипс.
Теперь единица совпадает — R10 у нас = R10 у них.

**Что сделано**
- hub: `_pip_size` → `_point_size`, формула `10^-(decimals-1)` → `10^-decimals`.
  EUR/USD: 0.0001 → 0.00001, USD/JPY: 0.01 → 0.001.
- front: `pipSizeOf` → `pointSizeOf` (та же смена степени); метка прогресса
  теперь в поинтах, целым числом («6/10» вместо «0.6/10»).
- Пресеты селектора укрупнены: [5,10,20] → [10,20,50] поинтов (иначе R10=1 пипс
  дало бы сотни баров/день). R10=1 пипс, R50=5 пипсов.
- Тесты: point_size вместо pip_size; sync-тест переведён на R:50 (=прежние
  0.0005, ожидания сохранены). Комментарии «пипсы»→«поинты».

Проверка: R:10 поинтов EUR/USD — медиана размаха ровно 10 поинтов, макс 17
(гэп-логика держит). Полная регрессия 81 тест — зелёная.

---

## 2026-07-17 — Рэндж-бары: быстрый бэкфил хвоста (ветка `range-bars`)

**Проблема.** Первый выбор R:N думал ~11 с: бэкфил читал ВЕСЬ тиковый архив
(80 файлов, 196 МБ, 2.6 млн тиков), хотя для 2000 баров R:10 нужны тики за
~1.4 дня (последние 2-3 файла). 97% работы впустую.

**Решение — `backfill_tail`.** Читает только хвост архива:
1. Пробник — последние ~4 дня, измеряет плотность баров/день для этого R.
2. Если хватило (мелкий R) — готово. Иначе из плотности оцениваем нужное число
   дней и читаем ровно их одним прогоном (не слепой геометрический рост —
   максимум 2 чтения). Отсечение старых файлов — по дате в имени (since_ts),
   дёшево.
Хвост совпадает с полным прогоном до тика: путь-зависимость якоря затухает за
несколько дней. Живой (недостроенный) бар может отличаться на 1 тик из-за среза
по границе суток — он тут же обновляется потоком, поэтому не важно.

**Результат:** R:10 — 11.2 с → **0.7 с** (×16). R:50 — 8 с, крупные R (мало
баров даже на всём архиве) — не хуже полного. Хаб переведён на backfill_tail.

Тесты: `test_backfill_tail_matches_full` (сверка хвоста с полным). Синтетический
архив в test_range_hub переведён на реальные ts (BASE=2026-07-01), иначе
since_ts-отсечение по дате файла выкидывало все тики. Регрессия 82 теста зелёная.

---

## 2026-07-18 (сб) — Рэндж-бары влиты в master, прод на едином дереве

Раздел рэндж-баров закрыт (владелец проверил, всё устраивает). Финал:

- `range-bars` → master fast-forward (`4108ca4`). Вся работа сессии в master:
  рэндж-бары (ядро+хаб+фронт), тип отрисовки «бары», фикс фулскрина, единица
  в поинтах, гэпы, быстрый бэкфил хвоста.
- Боевые юниты `chart-hub`/`chart-feed` переведены с worktree
  `terminal-phase2` на **master-дерево** `/root/projects/terminal`
  (WorkingDirectory + ExecStart). Бэкапы юнитов: `*.service.bak-worktree`.
- Рестарт в субботу (форекс закрыт — дыр в секундных ТФ нет). Хаб поднялся
  из master, история восстановлена, фид переподключился. Рэндж R:10 из нового
  дерева — 1.9 с, гэп-логика держит (макс 18 поинтов).
- Worktree `terminal-phase2` и `terminal-rangebars` сняты, ветки
  `range-bars` и `phase-2-refactor` удалены (слиты). Остался один master.
- Пробный фронт 8084 (chart-frontend-trial) погашен — прод-фронт 8082 отдаёт
  master.

**Долг (не в этой сессии):** CLAUDE.md всё ещё описывает монолит `server.py`/
сервис `chart` как боевой — расхождение накопилось за Фазу 2. Переписать под
хаб+фид отдельной задачей.

---

## 2026-07-18 (сб) — Фаза 4: 24/7 надёжность (завершена)

Четыре пункта ROADMAP, все в проде (рестарты в субботу, форекс закрыт):

1. **Watchdog фида** (`feeds/fxcm_feed.py`): поток следит за `_last_tick_ts`;
   тишина > 120 с в ОТКРЫТЫЙ рынок → форс-реконнект FXCM (лечит «тихое
   зависание»: TCP жив, тиков нет — systemd/reconnect-по-исключению не ловят).
   `market_open` гасит ложные срабатывания на выходных.

2. **Health-эндпоинт**: `hub.health()` (status ok/stale, uptime, data_age,
   clients) по HTTP `/health` на :8787 (aiohttp, отдельный порт, боевой WS не
   трогает), 200/503 по свежести данных для внешнего мониторинга.

3. **Heartbeat браузеру**: хаб шлёт health-снимок всем клиентам раз в 10 с.
   Фронт — индикатор связи (`#connDot`: live/idle/stale/down), watchdog по
   таймауту, автопереподключение WS с backoff (раньше onclose не было — вкладка
   висела на мёртвом сокете).

4. **Structured-логи**: `core/logfmt` — единый формат `LEVEL [component] msg`,
   print → logging в hub/feed/bus.

Общее: `core/market_hours.forex_open` (часы форекса) — общий хелпер фида и хаба.
Новые тесты: `test_feed_watchdog` (8), `test_hub_health` (5). Регрессия зелёная.

---

## 2026-07-18 (сб) — Брифинг: подпроект briefing/ (Фаза 7, слои 1-3 в проде)

Брифинг переделан в подпроект `briefing/` с памятью и раздельными мнениями.
Концепция: не советник, а картина + консенсус агентств + мысль DeepSeek.

- **sources.py** — живые фиды (8→66 новостей), диагностика по каждому, порог
  «мало новостей», календарь ForexFactory напрямую, время UTC+5.
- **memory.py** — журнал прогнозов + самооценка (сверка с market.db) на ДВЕ
  стороны (консенсус vs DeepSeek) + track_record «кто чаще прав».
- **technical.py** — техкартина из market.db (перенос 1:1).
- **prompt.py** — по паре РАЗДЕЛЬНО: консенсус аналитиков (consensus_direction/
  view) и мысль DeepSeek (direction/deepseek_view, согласен/расхождение).
- **agent.py / run.py** — вызов DeepSeek + точка входа.
- **Фронт** — панель показывает консенсус, мысль DeepSeek (тег согласие/
  расхождение), трек-рекорд обеих сторон внизу.
- **Крон** переключён на `python3 -m briefing.run`; fetch_news-крон убран
  (календарь в sources). Старьё → archive/old_briefing/, context.json→archive.

cheap-llm использован для валидации промпта (прогон в роли брифера). 24 теста.
Проверено end-to-end на живом DeepSeek + headless-панель. Осталось (7.2):
настройка под себя — модель/Telegram/набор пар, по факту использования.

## 2026-07-24 — Индикатор ATR + авторисовалка «Крюки Росса»

Две фичи фронта (статические файлы, рестарт сервисов не нужен — только refresh).

- **ATR** (`js/indicators.js` + `index.html`): `IndicatorMath.atr(data, period)` —
  True Range + сглаживание Уайлдера (сид = SMA(TR[1..period]), дальше рекуррентно),
  ровно как RSI по паттерну. Новый тип осциллятора `'atr'` заведён во всех точках
  `IndicatorManager` (легенда, `_isOscillator`, `recomputeAll`, `updateLast`,
  `_tailLen`, `_rebuildOscillators`). Ключевое отличие от RSI/Stoch: под-пана
  ATR в ЦЕНОВЫХ единицах → БЕЗ `fixedScale(0,100)`, свободный автоскейл. UI:
  `IND_DEFAULTS.atr` (period 14, #26c6da), меню добавления, настройки period.
  Проверено численно: варминг-nulls, сид на баре period, значения >0 и конечны.

- **Крюки Росса** (`js/rosshooks-overlay.js`, новый — по образцу
  `OrderbookOverlay`): канвас-оверлей z-index 880 (над стаканом, под рисунками),
  свой детектор. Правило: up-run из `minRun` подряд обновлений хая, следующий бар
  НЕ обновил → бычий крюк по хаю пика; зеркально по лоу → медвежий. `minRun` (деф 3)
  — ручка «канон ↔ любой разворот». Активные (уровень не пробит) — сплошная линия
  до правого края + метка RH; пробитые — по умолчанию скрыты (`showBroken:false`),
  включаются `setParams`. Ничего не хранится, пересчёт на каждый новый бар.
  Тумблер «Крюки Росса» в меню настроек (глобальный `layout.showRossHooks`),
  работает на ЛЮБОМ инструменте (нужен только OHLC). Детектор проверен численно
  на бычьем/медвежьем сценариях (пробитые и активные крюки).

Оба файла и инлайн-JS index.html проходят node --check. Headless smoke не гонял
(puppeteer-core не установлен). Не коммичено — жду проверки владельцем.

## 2026-07-24 (правки по ходу проверки)

- **ATR «нет значений»** — под-пана ATR создавалась без priceFormat инструмента,
  дефолт LWC=2 знака → форекс ATR 0.0004 рисовался как "0.00". Фикс: ATR-серия
  наследует precision/minMove свечной серии + `IndicatorManager.applyPriceFormat`
  синхронит формат при смене инструмента; значение ATR добавлено в легенду.
- **Огромная пана осцилляторов** — `setHeight(px)` LWC v5 игнорирует (раздаёт
  высоту stretch-факторами). Заменено на `_applyDefaultStretch`: каждый
  осциллятор = 20% total, основная пана = остаток. Ключ хранилища размеров
  сменён `paneStretch`→`paneStretch2` (старые битые ≈50/50 факторы отброшены).
- **Граница пан** — `--chart-sep` был бледный, усилен (свет #9aa1ab / тёмн #4a535e).
- **Крюки Росса: фильтр тренда** — детектор ловил up- и down-крюки независимо и
  везде → противотрендовый шум (down-крюк в ап-тренде на откате и наоборот).
  Добавлена привязка к тренду: бычий крюк только на higher-high пике, медвежий
  только на lower-low впадине (сравнение с предыдущим пиком/впадиной). Даёт и
  «сначала тренд, потом первый RH». Проверено на синтетике ап/даун-тренда с
  коррекциями: встречные крюки не появляются.

## 2026-07-24 — Крюки Росса переписаны на свинг-структуру

Владелец указал: прежний детектор просто метил побарные хаи/лои, «период
непонятен». Разметка Джо Росса стоит на СВИНГАХ, а не на побарных экстремумах.
Полностью переписан `js/rosshooks-overlay.js`:

1. **ЗигЗаг по порогу** `swingMult × ATR` → чистые свинг-пивоты H/L. Порог =
   «период» разметки, вынесен в UI (Порог свинга ×ATR, деф 2.0).
2. **Классификация** HH/HL/LH/LL относительно предыдущего одноимённого свинга.
3. **1-2-3** с проходом ПО СОСТОЯНИЮ ТРЕНДА: формация регистрируется только если
   разворачивает текущий тренд (top в апе / bottom в дауне), скан идёт от точки 3
   без перекрытий. Уровень точки 2 = SELL/BUY.
4. **RH** — трендовые крюки-продолжения по сегментам между разворотами (даун →
   swing Low, ап → swing High), пробитые/активные.
5. **RHR** — точка 3 формации (крюк разворота). ПРЕДВАРИТЕЛЬНО: определение RHR
   самое неоднозначное в методе, ждёт уточнения владельца.

Проверено численно на синтетике с двумя разворотами: свинги, HH/HL/LH/LL и обе
главные 1-2-3 (top 120→112→118, bottom 98→108→101) распознаны; после гейта по
тренду лишних формаций-продолжений и дублей RH/RHR нет.

**UI:** кнопка RH в тулбаре открывает меню (мастер + слои структура/1-2-3/RH/RHR
+ порог). Слои и порог переключаемы, состояние в `layout.rossHooks`. Старая
строка в меню графика убрана ранее.

Не коммичено. Визуальную проверку на реальных данных делает владелец.

## 2026-07-24 — Сетка Фибоначчи

Новый инструмент рисования `fib` в существующей подсистеме (DrawingEngine +
DrawingController + storage + контекстное меню), кнопка в тулбаре обеих панелей.

Модель: сетка задаётся ДВУМЯ точками (p1 = 0%, p2 = 100%), цена уровня =
`p1 + (p2 - p1) * value`. Значения > 1 (1.272 / 1.618) — проекции ЗА вторую
точку, как в TradingView. Каждый уровень — объект `{value, color, visible}`.

Что умеет (по ТЗ владельца):
- **Отключение линий** — галочка видимости у каждого уровня.
- **Свои уровни сверх умолчания** — ввод доли (0.886) или процентов (88.6),
  дубли не плодятся, список сортируется; лишние удаляются крестиком.
- **Автопродление вправо** — флаг `extendRight` (по умолчанию вкл), уровни
  тянутся до правого края паны.
- **Цвет каждой линии отдельно** — свой color-picker в строке уровня.
Плюс: заливка между уровнями (опция), опорная диагональ p1→p2, подписи
«процент + цена» с точностью инструмента.

Дефолт: 0 / 0.236 / 0.382 / 0.5 / 0.618 / 0.786 / 1 видимы, 1.272 / 1.618
добавлены, но погашены.

Попутно исправлено: `deleteSelectedLine` не удалял overlay-объекты из
`drawingEngine.drawings` (касалось и линий/зон) — «Удалить» на них не работало.

Проверено: математика уровней в обе стороны хода (0%=старт, 100%=конец,
161.8% = проекция), полный круг save/restore (погашенные уровни, свои цвета,
кастомный 0.886, флаги). Все затронутые файлы проходят node --check.

## 2026-07-24 — R-шкала рэндж-баров в TV-совместимую

Владелец: показывать R как в TradingView (BTC R:1000 → 100R), но НЕ сломать
форекс. Разобрались, откуда ×10: TV меряет R крипты в ЦЕЛЫХ единицах цены
(BTC шаг 1.0), а мы — в тиках (0.1). Форекс у TV меряется в пипеттах = тик,
т.е. наша шкала там УЖЕ совпадает.

Решение (только фронт, хаб/бэкфил/rangeTf НЕ трогаем — внутри всё в поинтах):
- rTicksPerTV(sym): форекс → 1, крипта → round(1/тик). BTC=10, ETH=100.
- pointsToTV / tvToPoints / formatRLabel — перевод для ЯРЛЫКА и ВВОДА.
- Ярлыки всех R-опций селектора считаются в TV-шкале; relabelRangeOptions
  переименовывает их при смене инструмента и при приходе instruments
  (фактор зависит от точности).
- Кастомный ввод «R…» теперь в TV-числах (BTC — 100, не 1000), дробь форекса
  (2.5) сохраняется.

Проверено численно: BTC 1000↔100R, ETH factor 100, форекс factor 1 (не тронут,
EUR/USD R10, USD/JPY R20, 2.5 сохранена), фолбэк без precision = 1, round-trip.

ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ: правило «крипта = целая единица» точно для дорогих
монет (BTC/ETH). Для дешёвых (LIT ~$1, тик 0.0001 → factor 10000) число R
выходит мелким (R0.1) и может расходиться с шагом R, который TV берёт для
таких монет. Если всплывёт — override фактора на инструмент.

## 2026-07-27 — Рэндж вынесен из селектора ТФ в кнопку R + Авто-R по ATR

Перестройка UI рэнджа по просьбе владельца.

- Селектор ТФ — ТОЛЬКО временны́е ТФ. Удалены пресеты R, опция «R…»,
  ensureRangeOption/ensureTfOption/relabelRangeOptions/RANGE_PRESETS.
- Кнопка R в тулбаре обеих панелей → меню: тумблер «Рэндж-бары вкл/выкл»
  (выкл → возврат на последний временно́й ТФ, lastTimeTf) + режим Авто/Ручной.
- Рэндж-режим принадлежит ПАНЕЛИ (layout.pane*.rangeOn), R — ИНСТРУМЕНТУ
  (layout.rangeTf[sym]). onopen/switchSymbol восстанавливают по rangeOn.
  Миграция: старый tf="R:.." без rangeOn → включаем.

Авто-R по ATR:
- R = k×ATR, k=1 (Renko/TV дефолт). Базис ATR — ВСЕГДА M5 (единый для подбора
  и дрейфа; по рэндж-барам ATR бессмыслен, M5 стабилен). M5-историю берём
  разовым запросом через ВРЕМЕННЫЙ WebSocket (у хаба нет history без смены
  подписки, трогать пану нельзя), кэш 30 с. Хаб НЕ тронут.
- «Авто» = разовый подбор (фиксирует R, классика) + запоминает M5-ATR как
  базлайн. Кнопка R подсвечивается (.r-alert), когда дрейф ATR >1.5× — «пора
  пере-подобрать». «Подобрать сейчас» показывает превью (R100 → R140) с confirm.
- «Ручной» — ввод R в TV-шкале.

Проверено численно: ATR→R (BTC $60→600pts=R60, EURUSD 0.0004→R40), порог
дрейфа (1.5 не алерт, 1.67 алерт, 0.5 алерт). Хаб на set_tf M5 шлёт
type:"history" с data — временный WS ловит. node --check зелёный, dead refs
нет, фронт отдаёт файл (curl). Headless chromium в песочнице не стартует —
визуал проверяет владелец.

## 2026-07-27 — Режим KVKK для размера рэндж-бара

Источник формулы — дневник Mr Cvokka (проект Litr_v_odnogo, docs/kb_mrcvokka_diary.md,
стр. :16 и :80): «размер ренж-бара ~30% от среднего размера минутной свечи за сессию».
Владелец уточнил формулировку (не ADR, не M1-дневной, а именно 0.3×средняя M1).

Добавлен ТРЕТИЙ режим кнопки R — KVKK, рядом с Авто и Ручным:
- Auto: R = 1×ATR(M5) (как было).
- KVKK: R = 0.3 × средний размах (High−Low) свечи M1 за окно. Крипта 24/7 —
  жёстких сессий нет, поэтому «за сессию» = скользящее окно M1 длиной сессии
  (KVKK_WINDOW=480 ≈ 8 ч), а не календарные Азия/Европа/Америка.
- Manual: ввод в TV-шкале.

Рефактор: подбор обобщён под режимы. _fetchTfHistory (временный WS, любой ТФ) →
fetchSizingMetric(sym, mode) отдаёт метрику (auto=ATR M5, kvkk=ср.M1), кэш 30с
по ключу mode:sym. metricToPoints(v, mode) применяет k (1.0/0.3). Дрейф/подсветка
и превью-подтверждение работают для обоих авто-режимов (метрика своего режима).
Хаб не тронут.

Проверено численно: режимы дают разный R (BTC KVKK R7.5 vs Auto R60), окно <480
берёт всё. node --check зелёный, dead refs нет.

## 2026-07-31 — Автобрифинг остановлен

По распоряжению владельца сняты все три крон-задачи `python3 -m briefing.run`
(04:00 / 12:00 / 17:00 локального, UTC+5). Строки не удалены — закомментированы
с пометкой даты, включить обратно = снять `#`. Бэкап прежнего crontab —
в скретчпаде сессии (`crontab.bak`). Хаб не тронут: `briefing_watcher` жив и
продолжит отдавать на фронт последний `briefing.json`, новых просто не будет.

## 2026-08-04 — Индикатор всплесков (shock) + починка календаря

Ветка `hypothesis`. Задача: сигнальщик по четырём форекс-парам, EUR/USD —
ведущая, остальные ведомые.

**Исследование (hypothesis/):** проверял гипотезу «после всплеска UJ следующая
минута разворачивается». За две недели при пороге 10σ — 7 событий, разворот
42.9% против БАЗОВОЙ частоты смены цвета 52.0% (отклонение 0.5 s.e.). Эджа нет,
и это записано в docstring детектора: маркер отмечает необычную свечу, а не
предсказывает направление. Тот же вывод по вчерашней Америке (13:00–17:00):
дивергенции корреляции живут в основном в CHOP (46%), т.е. шум.
M1 за две недели резались из тикового архива — `market.db` держит окно 2000
баров (~33 ч), двух недель там нет.

**Калибровка порогов** (`hypothesis/calibrate_sigmas.py`, 21.07–03.08): 8σ
нельзя переносить на все пары — p99.9 у пар разный. USD/JPY 8.0 (закреплено
владельцем), AUD/USD 5.0, USD/CAD 4.5. При едином 8σ USD/CAD молчал бы всегда
(его максимум за две недели — 6.6). Сигма меряет РЕДКОСТЬ внутри пары, не размер
движения.

**Починен загрузчик календаря.** Диагноз: `data_loaders` тянул
`ff_calendar_nextweek.json` → HTTP 404 (ForexFactory убрал эндпоинт), файл
замёрз на 14–15 июля. Живой только `thisweek` — его и использует briefing, но
результат никуда не сохранялся. Новый `data_loaders/sync_calendar.py` тянет
thisweek и МЕРЖИТ в CSV (не перезаписывает — иначе история недели терялась бы),
атомарная запись + ретраи: у ForexFactory жёсткий рейт-лимит, 429 держался ~5
минут. Крон 4×/сутки (03:10/09:10/15:10/21:10), первый — до брифинга.

**Реализация:**
- `core/shock_detector.py` — z-score диапазона против своих 30 свечей, пороги
  на пару, ведущая должна быть спокойна (<2σ), разрывы в истории отбрасываются.
- `core/news_windows.py` — окна тишины: сессии ±30 мин, новости ±15 мин.
  Новость гасит только свои валюты (BOC → USD/CAD, не EUR/USD).
- `hub.py` — `_shock_check` на ЗАКРЫТОЙ M1 (у живой диапазон ещё растёт),
  рассылка в wire-имени клиента как у алертов, журнал на сутки + `get_shocks`.
- `index.html` — маркер (стрелка с σ), кликабельный попап с переходом на
  инструмент, бейдж окна тишины. `shock`/`shocks` добавлены в `BROADCAST_TYPES`
  — без этого фильтр requestId отбросил бы их молча.
- `js/storage.js` — маркеры сохраняются как рисунки, переживают F5.
- Маркеры всплесков в ОТДЕЛЬНОМ плагине от сигнальных: один слой на серию
  перезаписывался бы целиком.

**Проверено:** `tests/test_shock_detector.py` (реплей архива + guard-кейсы) —
все зелёные, py3.7/3.10 компиляция, `node --check`. Прогон
`hypothesis/preview_signals.py`: 2.3 звуковых сигнала/день (USD/CAD 14,
USD/JPY 10, AUD/USD 8 за две недели), 14 погашено сессионными окнами.

**НЕ СДЕЛАНО:** `chart-hub` НЕ перезапущен — в проде пока старый код, индикатор
не работает до рестарта (правила запрещают без явного разрешения). Новостных
подавлений в прогоне ноль: календарь начал накапливаться только сегодня,
на исторический период событий в нём почти нет — крон это исправит сам.

### Правка: сигнал по ФОРМИРУЮЩЕЙСЯ свече (то же 04.08)

Владелец: ждать закрытия — опаздывать на минуту, сигналить надо живую свечу,
как только её диапазон перешагнул порог. Хвост после отката — тоже диапазон,
сигнал остаётся в силе.

- `evaluate_live()` — скоринг живой свечи, ОДИН сигнал на минуту: диапазон
  только растёт, иначе каждый следующий тик всплеска давал бы новый попап.
- Живая свеча в историю НЕ попадает (`push_live` отдельно от `push`) — её
  растущий диапазон испортил бы статистику, против которой сама считается.
- `leader_z` смотрит на ЖИВУЮ свечу ведущей: пока ведомая формируется, EUR/USD
  тоже ещё не закрылась.
- Закрытие оставлено добором: свеча, перешагнувшая порог на последних тиках,
  живым путём не успевает. Дубль исключён (`_fired_at`).
- Общий скоринг вынесен в `_score()` — оба пути применяют одни пороги.

Найден и исправлен баг: локальная переменная `log` для журнала затеняла
модульный логгер, следующий же `log.info()` упал бы с AttributeError прямо
в обработке тика. Переименована в `journal`.

Тесты дополнены живыми кейсами. Три упали не из-за кода: фикстуры давали 30
свечей с ОДИНАКОВЫМ диапазоном → stdev=0 → `range_zscore` честно возвращал
None. Дан разброс, добавлен контрольный тест «при спокойной ведущей та же
свеча срабатывает» — без него проверка подавления ничего не доказывала.

Проверка на реальных тиках (`hypothesis/replay_live.py`, 5 дней): 35 событий,
ВСЕ пойманы внутри минуты, добор не понадобился ни разу. Медиана — 39-я
секунда, т.е. на ~21 с раньше закрытия; резкие всплески ловятся на 1-й секунде.
Появились события, которых не было по закрытой свече (USD/JPY 09:49 03.08,
13.6σ): свеча откатывалась к закрытию, и итоговый диапазон уже не дотягивал —
тот самый случай с хвостом.

### Панель сигналов на кнопке «Сигналы» (то же 04.08)

Владелец: пока сигналов нет, движок никак не проверить — привязать к кнопке
«Сигналы» панель со списком и переходом к свече по клику.

Кнопка ПЕРЕИСПОЛЬЗОВАНА, а не добавлена вторая: `toggleSignalPanel` вела на
отключённый с Фазы 2.1 контур, `get_signals` в хабе нет вовсе — `_signalLog`
всегда пуст, нажатие не показывало ничего. Старые функции оставлены
объявленными (не сломаны), кнопка теперь зовёт `toggleShockPanel`.

Панель: пустое состояние объясняет, что движок жив, и печатает пороги — иначе
тишину не отличить от поломки. Список свежих сверху, подавленные серым с
причиной, клик — переход на инструмент и центрирование.

**Найдены и исправлены два бага прошлой правки** (оба ловятся только в
браузере, статические проверки их не видели):

1. `st.symbol` — такого поля в проекте НЕТ (обе панели всегда показывают
   `currentSymbol`). Условие `st.symbol !== ev.symbol` было всегда истинным,
   и маркеры не появились бы НИКОГДА. Проверено в headless: до фикса 0
   маркеров, после — 1.
2. Сравнение символов без нормализации: хаб шлёт wire-имя клиента, и
   `fxcm:AUD/USD` против `AUD/USD` считались разными инструментами — лишний
   полный `switchSymbol` с перезагрузкой истории вместо центрирования.
   Везде теперь `displaySymbol()`.

Проверено в headless Chromium (CDP, без puppeteer): открытие/закрытие панели,
пустое состояние, живой сигнал, подавленный сигнал, переход между
инструментами (AUD/USD → EUR/USD, целевая свеча попала в видимый диапазон),
маркер + сохранение в localStorage. Ошибок на странице ноль, график цел.

### Форма всплеска: рывок против размазанного движения (то же 04.08)

Владелец посмотрел графики: широкая M1 из равномерного шума — не событие,
нужен резкий рывок внутри минуты. Триггерная единица — 10 секунд.

**Замер до реализации** (`hypothesis/burst_concentration.py`, 2 недели).
Сравнил два определения окна:
- ФИКСИРОВАННЫЕ слоты (0-10, 10-20, …) режут всплеск на границе: 21.07 01:29
  дал 48% вместо 83%, 22.07 21:46 — 48% вместо 85%, 03.08 14:16 — 69% вместо
  89%. Из 6 самых резких событий фиксированные слоты испортили 4.
- СКОЛЬЗЯЩЕЕ окно (любые 10 с) — верное по смыслу «максимум за 10 секунд».
Взято скользящее.

Распределение концентрации (звуковые всплески, 2 недели): ≥60% — 14, ≥70% — 9,
≥80% — 6, ≥90% — 2. Владелец выбрал **70%**.

**Форма НЕ фильтрует, а классифицирует** (решение владельца): оба вида звучат
и ставят маркер, но помечаются по-разному — «⚡ рывок» против «〜 размазан»,
с процентом покрытия. Отбрасывать молча владелец не захотел.

**Проверено расхождение живой формы с итоговой**
(`hypothesis/verify_shape_live.py`): 60 совпадений из 67, 7 переклассификаций,
ВСЕ в одну сторону burst→spread. Механизм: в момент срабатывания тиков ещё
мало, «100% за 10 с» к закрытию оказывается размазанным (30.07 13:36: 100% →
45%). Обратных ошибок нет — резкие события не занижаются. Поэтому форма
УТОЧНЯЕТСЯ на закрытии минуты (`_shock_refine_shape`) и уточнение уходит на
фронт: в журнале остаётся правда о минуте.

Тесты: добавлены кейсы на форму. Один упал не из-за кода — тестовая база
времени не кратна 60, а `push_tick` считает бакет из времени тика. В проде
время свечи всегда выровнено. Выровнял ВСЕ базы в файле (4 из 5 были кривые) —
мина сработала бы при любой следующей правке.

Проверено в headless: обе формы рисуются на маркере и в панели, график цел.
Хаб перезапущен, все четыре пары получают тики.

### Ведущая пара — метка, а не блокировка; пороги снижены (то же 04.08)

Повод: владелец попросил проверить USD/JPY 04.08 11:56. Свеча 204 пипса
(среднее 41), обвал 91% за 10 секунд — но сигнала не было ПО ДВУМ причинам:
сигма 6.3 при пороге 8.0 И EUR/USD шумел (2.18σ при лимите 2.0).

Решение владельца: сигму снизить, лимит ведущей оставить, но сделать
УСЛОВНЫМ — не блокировать, а классифицировать: SOLO (двинулась только эта
пара) против IN STREAM (несло общим движением доллара).

**Замер до реализации** (`hypothesis/tune_sigma_solo.py`, 2 недели). Снятие
блокировки само по себе утраивает поток: 14.4 события/день против 2.3.
Большинство всплесков — общий поток, SOLO редка (у USD/JPY при 6σ — 22 из 60,
у USD/CAD при 6σ — 1 из 16). Владелец выбрал JPY 6.0 / AUD 4.5 / CAD 4.5
(~11/день) и звук ТОЛЬКО на рывки (~4–5/день), размазанные — молча в панель.

Пороги были p99.9 (8.0/5.0/4.5) при блокирующей ведущей. Теперь ведущая не
фильтрует, и пороги несут всю нагрузку — отсюда снижение.

Реализовано: `origin` = solo / in_stream / unknown (unknown — когда данных по
ведущей нет; состояние НЕ угадывается). Решение о звуке принимает хаб
(`audible`), фронт его исполняет — иначе логика размазалась бы по двум местам.
Форма уточняется на закрытии вместе с `audible`: событие, оказавшееся
размазанным, не должно остаться «звучавшим» в журнале.

Тесты переписаны: старый кейс проверял, что шумная ведущая ПОДАВЛЯЕТ сигнал —
это поведение отменено. Теперь проверяются три исхода метки и то, что событие
приходит в любом случае. Реплей архива: 157 событий против прежних 32.

Эталонная свеча 11:56 проверена после правки: 6.3σ прошла порог 6.0, метка
IN STREAM, форма burst 91%, звук — да.

### Фикс: панель теряла историю сигналов после F5 (то же 04.08)

Вопрос владельца «показывает ли фронт прошедшие сигналы» вскрыл баг.
Проверка в headless: после перезагрузки страницы маркеры на графике на месте
(все 3), а панель писала «Сигналов нет» — при том, что данные лежали рядом.

Причина: панель читала `shockLog` — массив в памяти вкладки, который F5
обнуляет. Восстанавливался он только из журнала хаба (`get_shocks`), а тот
тоже жил в памяти и обнулялся рестартом хаба. Маркеры при этом хранятся в
localStorage и поднимались штатно, то есть данные были — панель их не смотрела.

Исправлено на двух уровнях:
- `restoreShockLogFromDrawings()` во фронте — журнал панели поднимается из
  сохранённых маркеров (обе панели + все инструменты из layout), зовётся при
  открытии панели и при загрузке истории;
- журнал хаба ПЕРСИСТИТСЯ (`shock_log.json`, атомарная запись, окно суток,
  `_shock_id` продолжается с максимума — иначе новые id конфликтовали бы со
  старыми во фронте). Раньше каждый рестарт хаба стирал историю сигналов
  начисто, и фронт с чистым localStorage оставался ни с чем.

Проверено после фикса: F5 → 6 строк в панели с метками SOLO/IN STREAM и
формой, маркеры целы. Файл под `*.json` в .gitignore — в репозиторий не идёт.

### Прогрев детектора из тикового архива; персист убран (2026-08-05)

**Найден баг, из-за которого сигналов не было вообще.** Владелец спросил, почему
за сутки ни одного — проверка показала: офлайн-детектор находит на тех же
данных 23 события, хаб — ноль.

Причина: в проекте ДВА диалекта свечи. `CandleBuilder` отдаёт
`open/high/low/close`, а БД и архивные скрипты — `o/h/l/c`. Детектор читал
только короткие ключи, `_range_of` возвращал 0.0 для КАЖДОЙ живой свечи,
сигма никогда не достигала порога. Сигнал был невозможен в принципе.
Офлайн-скрипты баг не ловили: там свечи собирались вручную с короткими
ключами. Та же ошибка была в `_shock_refine_shape`.
Исправлено: `_field()` понимает оба диалекта, добавлен регрессионный тест
на свечах формата хаба.

**Персист журнала убран, вместо него прогрев из архива.** Владелец:
«зачем копить историю, если можно взять из архива». Возражение справедливое —
`shock_log.json` был кэшем поверх данных, которые и так на диске, и вторым
источником правды: он бы разошёлся с архивом при первой смене порогов (ровно
как сегодня, когда копился сломанным детектором).

`warmup_from_ticks()` при старте поднимает 40 минут тиков и отдаёт детектору
готовую скользящую статистику. Тики, а не свечи из БД: форма всплеска
измерима только по тикам. Прогрев 4 пар — 0.62 с, все готовы к работе с
первой секунды вместо получаса немоты после каждого рестарта.
Формирующаяся минута в историю не кладётся — она испортила бы статистику.

Тест прогрева сначала упал (sigma=None) — дефект теста: целился в минуту
`anchor`, а история заканчивается на минуту раньше (формирующаяся
отброшена). В бою этого разрыва нет: живая минута закрывается штатно.

### UI-правки панели и маркеров + фикс селектора (2026-08-05)

По замечаниям владельца с 13" ноутбука.

1. **Промотка списка уже была** — `max-height` + `overflow-y:auto`, окно не
   растягивается. Высота поднята 320→420px вместе с остальным.
2. **Панель +30%**: ширина 300→390px, базовый шрифт 12→15px, все инлайновые
   размеры внутри (заголовок, бейджи, заметки о форме) — пропорционально.
3. **Маркер +50% и отодвинут**: `size` 1→1.5. Отступ сделан через позиции
   `atPriceTop`/`atPriceBottom` (LWC 5.0.4+, сверено по докам через context7) —
   у `aboveBar`/`belowBar` зазор фиксированный и маркер налезал на тело свечи.
   Отступ = 4% ВИДИМОГО диапазона, а не в пунктах: цены EUR/USD и USD/JPY
   различаются в 130 раз, фиксированный шаг выглядел бы по-разному.
   Для этого в маркер добавлены `high`/`low` свечи (4 места во фронте +
   2 в storage.js).
4. **Фикс: селектор символа не менялся при переходе по сигналу.**
   `symbolSelect.value` выставлялся ТОЛЬКО при инициализации, а `switchSymbol()`
   его не трогал — при программном вызове список показывал прежнюю пару,
   противореча графику. Исправлено в самом `switchSymbol`, т.е. для любого
   вызова. Сравнение по `displaySymbol` — значения пунктов могут быть с
   префиксом провайдера.

Проверено в headless: панель 390px/15px/420px, маркер `atPriceTop` с ценой
выше high свечи, переход AUD/USD→EUR/USD синхронизирует селектор.

### Маркер всплеска переделан: вилка из двух стрелок (2026-08-05)

Владелец: одна стрелка указывала В СТОРОНУ от свечи и читалась как
направление движения. Теперь свеча берётся В ВИЛКУ: `arrowDown` сверху и
`arrowUp` снизу, обе указывают на неё.

- Цвет один — `#1E88E5` (между голубым и синим). Направление и форма всплеска
  видны в панели, на графике маркер показывает только МЕСТО события. Цвет
  намеренно не зелёный/красный, чтобы не путался с цветом свечи. Подавленный
  окном тишины — приглушённый `#5a7a99`.
- Текст с графика убран (просьба владельца): σ и форма остаются в списке.
- Направление свечи на форму маркера больше не влияет — логика упростилась.
- Отступ прежний: 4% видимого диапазона через `atPriceTop`/`atPriceBottom`.

Проверено в headless: на событие ровно 2 маркера, верхний НАД high, нижний
ПОД low, цвет и размер 1.5 применены, текста нет.

### Фикс: переход по сигналу на сплит-графике (2026-08-05)

Владелец на сплите: переход к цене работал только на ПЕРВОМ графике, второй
приходилось мотать руками, маркер появлялся лишь со ВТОРОГО клика по сигналу.

Три симптома — две причины:

1. `centerOnTime` центрировал только `panesState[1]`. Теперь обе панели, окно
   считается для каждой отдельно (ТФ у них разные).
2. Маркер по ЧУЖОМУ инструменту отбрасывался при получении события (панели
   показывают только `currentSymbol`), а после `switchSymbol` его никто не
   создавал — отсюда «нужно нажать дважды». Логика привязки вынесена в
   `attachShockToPanes()` и вызывается ТРИЖДЫ: на живом событии, после
   центрирования по клику и в `centerOnShockWhenReady` (там же, где дожидаемся
   асинхронной истории нового инструмента).

Попутно: `get_shocks` при открытии панели слался только через сокет первой
панели — теперь через любой живой, журнал общий.

Проверено в headless со СПЛИТОМ (`layout.split=split`, вторая панель видима):
после ОДНОГО клика по сигналу чужого инструмента — символ переключён, свеча
в видимом окне ОБЕИХ панелей, маркеры 1/1 (по 2 стрелки на панель).

### Фикс: живой сигнал не рисовал маркер до нажатия кнопки (2026-08-05)

Владелец поймал ПЕРВЫЙ живой сигнал: попап и звук отработали штатно, а маркер
не появился — возник только после нажатия кнопки «Сигналы».

Диагноз (headless, замер состояния в момент события): панель может быть ещё
НЕ ГОТОВА — `series`/`chart` не созданы (`2: series=false chart=false data=0`).
`attachShockToPanes` такую панель пропускает, событие остаётся только в
журнале. Нажатие кнопки перерисовывало всё из журнала — отсюда «появился
после клика».

Исправлено с двух сторон:
- `syncShockMarkersFromLog()` — зеркало `restoreShockLogFromDrawings()`:
  панель, получив историю, сама подтягивает события журнала. Раньше поток был
  односторонним (рисунки → журнал), и обратно ничего не шло.
- `retryAttachShock()` — короткая серия попыток (20 × 300 мс) на случай, когда
  панель догружается уже после прихода события.

По дороге найден и исправлен дефект собственной правки: `attachShockToPanes`
возвращала `true`, если маркер лёг ХОТЯ БЫ на одну панель. При готовой первой
и грузящейся второй ретрай считал работу сделанной, и на второй панели маркер
так и не появлялся (проверено: до фикса 0, после — 1). Теперь успех — только
когда приняли ВСЕ существующие панели.

Проверено в headless: сигнал при неготовой панели → маркер появляется САМ
после её готовности, кнопку не нажимали. Регрессия по сплит-навигации не
обнаружена.

## 2026-08-06 — Режимы аккаунта: /profile, протокол переключения, фикс опасного бага

Владелец обновил INTRADE_API_MAP.md (§3.1, разбор /profile): переключатель
Реал⇄Демо `user_real_trade.php` — ТУМБЛЕР БЕЗ ПАРАМЕТРА, желаемое состояние
передать нельзя, вызов просто инвертирует счёт. Состояние читается только
парсингом /profile (`checked` на активном варианте, onclick — на
противоположном).

**Найден и исправлен опасный баг:** `client.active_trades()` — сверка
UNKNOWN-состояния — ходил на `/user_real_trade.php`. По ПЕРВОЙ версии карты
тот числился «списком активных сделок»; на деле каждая сверка потерянного
ответа МОЛЧА переключала бы тип счёта, возможно на реал. Переведён на
`/trade_load_more2.php` (last=0). Регрессионный тест ловит возврат бага.
Устаревшие упоминания исправлены в engine/journal/BOT_TZ/CLAUDE.md.

Реализовано:
- `AccountProfile` (models) + `parse_profile` (parsers): строгий разбор —
  для каждой пары radio ровно один checked, иначе PlatformError
  (`profile_ambiguous` / `profile_layout_changed`). Не угадывать: цена
  догадки — ставка реальными деньгами при конфиге demo.
- `client.profile()` — чтение с ретраями (идемпотентно);
  `_toggle_account_mode()` — приватный, ОДИН запрос без ретраев (как
  open_trade: не идемпотентен, повтор при потерянном ответе переключил бы
  счёт второй раз); `ensure_account_mode(target)` — протокол §3.1: читает →
  сравнит → переключит только при расхождении → перечитает и убедится.
- `engine._verify_account`: ПЕРВАЯ линия — /profile (account против конфига,
  trade_type == sprint, currency == usd), расхождение = жёсткий стоп без
  переключения на лету. Эвристика по балансу оставлена ВТОРОЙ линией:
  оба источника парсят HTML и хрупки по-разному.

Фикстуры profile_{demo,real,ambiguous}.html — по разметке из карты §3.1
(полного дампа /profile нет, в шапке пометка «заменить сохранённым»).
Тесты: test_bot_profile.py (17), test_profile_safeguard_hard_stop в движке.
Все наборы бота зелёные: engine 63, parsers 51, config 25, risk 54,
journal 52, panel 28, payout 60, strategy 32, hubfeed 24, profile 17.
