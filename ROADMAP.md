# Terminal — план работ (для исполняющего агента)

> Все решения ниже **согласованы с владельцем проекта**. Не пере-проектировать,
> не расширять объём. Если решение выглядит спорным — спросить, а не менять молча.
> Факты об API Lighter проверены живым прогоном 2026-07-12 — не додумывать.

---

## Правила работы с git (действуют на все фазы)

**Никогда не коммитить напрямую в основную ветку.**

### Ветка на фазу

Каждая фаза — своя ветка, от актуальной основной:

```bash
git checkout master && git pull
git checkout -b phase-1-storage      # phase-2-refactor, phase-3-lighter, ...
```

### Коммиты

- Коммитить **по логическим шагам**, не одной кучей в конце.
  Например в Фазе 1: схема БД → `core/db.py` → `core/candles.py` → тесты.
- Сообщение: что и зачем, а не «fix». Русский или английский — единообразно.
- **Не коммитить**: `*.db`, `*.db-wal`, `*.db-shm`, `data/`, `archive/`,
  `.env`, `__pycache__/`. Проверить, что они в `.gitignore` — если нет, добавить
  **до** первого коммита фазы.

### Пуш и слияние

1. Пуш ветки: `git push -u origin phase-N-name`
2. **Остановиться и показать владельцу**: что сделано, что изменилось, как проверить.
3. **Ждать явного подтверждения.** Не сливать по своей инициативе, даже если
   всё зелёное и приёмка формально пройдена.
4. Только после «да» — слияние в основную ветку и пуш.

### Откат

Пока фаза не слита, откат = смена ветки. Именно поэтому основная ветка всегда
должна оставаться рабочей: если новая архитектура сломается на живом сервере,
возврат к предыдущему состоянию должен занимать секунды, а не разбор дифов.

---

## Контекст

Терминал на LightweightCharts v5 + Python WS-сервер. Сейчас единственный источник —
FXCM (ForexConnect, 4 форекс-пары). Цель: мульти-провайдерный терминал с управляемым
окном истории, работой 24/7 и торговлей через API биржи Lighter.

**Состояние VPS (аудит 2026-07-12):** 26 ГБ свободно, места хватает — экономить
на хранении не требуется. Терминал занимает ~1.3 ГБ.

**Порядок фаз обязателен.** Каждая опирается на предыдущую. 24/7 невозможен, пока
рестарт теряет историю; история не переживёт рестарт, пока живёт в RAM. Поэтому
хранение — первое, а не последнее.

---

## Фаза 1 — Хранение и динамическое окно истории

### 1.1 Ключевое решение: ретеншен по числу баров, не по времени

Хранить **последние `KEEP_BARS = 2000` баров** на каждую пару
`(provider, symbol, tf)`. Окно скользит само, подрезается после каждой закрытой свечи.

Почему 2000, а не «2× видимой области»:
- видимая область = 122 бара (`DEFAULT_BAR_COUNT`), 2× ≈ 250;
- но `js/indicators.js` (`_tailLen`) требует хвост **300–400 баров** для EMA/RSI/MACD,
  иначе первые значения индикаторов врут;
- плюс запас на прокрутку влево.

Цена вопроса: 8 символов × 11 TF × 2000 баров × ~70 Б ≈ **12 МБ на всю базу**.
При 26 ГБ свободных обсуждать нечего.

### 1.2 Схема SQLite

```sql
PRAGMA journal_mode=WAL;

CREATE TABLE candles (
  provider TEXT, symbol TEXT, tf TEXT, time INTEGER,  -- unix СЕКУНДЫ
  o REAL, h REAL, l REAL, c REAL,
  vol_base REAL, vol_quote REAL,                      -- NULL для FXCM
  PRIMARY KEY (provider, symbol, tf, time)
) WITHOUT ROWID;

CREATE TABLE instruments (
  provider TEXT, symbol TEXT,
  price_decimals INTEGER, size_decimals INTEGER,
  min_base REAL, has_volume INTEGER,
  meta TEXT,                          -- сырой JSON провайдера
  updated INTEGER,
  PRIMARY KEY (provider, symbol)
);
```

PRIMARY KEY даёт идемпотентность: повторный бэкфил через `INSERT OR REPLACE`
не плодит дубли.

### 1.3 Подрезка окна

Вызывать после каждой **закрытой** свечи (не на каждом тике):

```sql
DELETE FROM candles
WHERE provider=? AND symbol=? AND tf=?
  AND time < (SELECT time FROM candles
              WHERE provider=? AND symbol=? AND tf=?
              ORDER BY time DESC LIMIT 1 OFFSET 2000);
```

`VACUUM` — раз в неделю, кроном. Крон-задачи по времени для ретеншена не нужны.

### 1.4 Тиковые CSV

`data/` (858 МБ, растёт ~11 МБ/сутки) — **оставить как есть**. Сырьё для будущих
бэктестов, места хватает. Сжатие и ротацию не делать: gzip сломает чтение из
существующих скриптов, а выигрыш не нужен.

### 1.5 Приёмка

- [ ] Рестарт сервера не теряет историю
- [ ] `SELECT tf, count(*) FROM candles GROUP BY tf` → ≤2000 на пару
- [ ] Фронт работает как раньше, визуально ничего не изменилось

---

## Фаза 2 — Расчистка и разбор монолита

Поведение не меняется. Только структура.

### 2.1 Выключить сигнальный контур

`SIGNALS_ENABLED = False` — один флаг, один if в `process_tick`.
**Код НЕ удалять** (`signal_engine`, `pattern_memory`, `signal_tracker`,
`structure_engine`) — владелец может вернуться к нему позже.

Побочный эффект, ради которого это делается рано: `market_memory.json` (72 МБ)
и `signal_log.json` (26 МБ) сейчас **перезаписываются целиком каждые 240 секунд**.
Это ~100 МБ записи на диск ради одной строки. Выключение флага убирает нагрузку.

### 2.2 Архивация (СТРОГО ПОСЛЕ 2.1)

```bash
cd /root/projects/terminal
mkdir -p archive/old_data
mv market_memory.json signal_log.json signal_stats.json \
   session_bias.json briefing_context.json archive/old_data/
mv History/ archive/old_data/
mv index_split.html archive/
```

- ⚠️ Если сделать `mv` до выключения флага — сервер пересоздаст файлы через 4 минуты.
- ⚠️ `briefing.json` НЕ трогать — читается живым сервером.
- ⚠️ `History/` — перед переносом проверить: `grep -rn "History" --include=*.py .`

### 2.3 Структура

```
terminal/
├── core/
│   ├── bus.py          # внутренний WS 127.0.0.1:8766 — фиды → хаб
│   ├── db.py           # SQLite: upsert, чтение, подрезка окна
│   └── candles.py      # нарезка тиков по TF + aggregate() (взять из server.py)
├── feeds/
│   ├── fxcm_feed.py    # py3.7 venv (forexconnect)
│   └── lighter_feed.py # py3.10 venv (websockets + raw REST)
├── common/
│   └── lighter_raw.py  # _normalize_trade, Deduper, ping/pong — ШАРИТСЯ С БОТОМ
├── hub.py              # py3.10: bus → нарезка → SQLite → WS :8765 → браузер
├── retention.json      # KEEP_BARS, белый список рынков
└── archive/
```

**Почему процессы разделены:** `forexconnect` требует Python **3.7**, SDK Lighter —
**3.8+**. В одном процессе несовместимы. На VPS есть 3.10. Бонус: падение одного
фида не роняет другой.

### 2.4 Контракт шины

```json
{"type":"tick","provider":"lighter","symbol":"BTC",
 "ts":1783869420,"price":64132.6,"size":0.0123}

{"type":"instruments","provider":"lighter","data":[
 {"symbol":"BTC","price_decimals":1,"size_decimals":5,
  "min_base":0.0002,"has_volume":true,"meta":{}}]}
```

FXCM шлёт то же с `"size": null`.

**Время везде в СЕКУНДАХ.** Lighter отдаёт миллисекунды — конвертировать на границе
фида (`ts // 1000`). Внутрь системы мс не пускать.

Хаб при нарезке: суммирует `size` → `vol_base`, `size * price` → `vol_quote`.
При `size = null` объём остаётся `null`.

### 2.5 Фронт

- ✅ Новое WS-сообщение `instruments` при коннекте → `priceFormat: {precision, minMove}`
  берётся оттуда. Захардкоженные `precision: 5` убраны (2026-07-15, USD/JPY=3).
- ✅ Селектор заполняется из `instruments`, а не из HTML-разметки (2026-07-15).
- ⏸ Символы `fxcm:EUR/USD`, `lighter:BTC` — **ОТЛОЖЕНО до Фазы 3**. Префикс нужен
  только при втором провайдере (коллизия `BTC` у двоих). Сейчас он лишь сломал бы
  localStorage и UI (currentSymbol в 22 местах index.html), а под Lighter (другой
  формат символов) его всё равно придётся переделывать. Делать вместе с Фазой 3.

### 2.6 Миграция без простоя

1. `core/*` — юнит-проверка нарезки на сохранённом CSV из `data/`. Живой сервер не трогать.
2. `hub.py` поднять на порту **8767** параллельно со старым `server.py` — **через
   `git worktree`**, не переключая рабочее дерево `/root/projects/terminal`:
   ```bash
   git worktree add ../terminal-phase2 phase-2-refactor
   ```
   systemd запускает `server.py` **по пути**, а не по ветке — какой код выкачен в
   `/root/projects/terminal`, тот и исполняется. Урок Фазы 1 (`LOG.md`,
   2026-07-14): рабочее дерево осталось на feature-ветке, systemd после
   автоперезапуска подхватил чужой код, он падал на отсутствующем `_sqlite3`,
   счётчик рестартов дошёл до 2529, порт 8765 умер.
3. Проверять прод-код командой **`python3.7`**, не `python3` (3.10). На проде
   `python3.7` собран без штатного `_sqlite3` (модуль досборен вручную) — зелёные
   тесты на 3.10 ничего не говорят о том, что реально исполнится под systemd.
4. `feeds/fxcm_feed.py` — вынести стриминг. Сверить: данные в хабе идентичны старым.
5. Переключить фронт на хаб. Старый `server.py` → `archive/`.

---

## Фаза 3 — Lighter как источник данных  ✅ В ПРОДЕ с 2026-07-21

Влита в master (`5ded560`), 21 коммит. Прод: 4 сервиса, 15 инструментов
живьём (4 форекс + 11 крипто). Реализовано сверх исходного плана: дельта
(агрессорный дисбаланс) в свече и на рэндж-барах, лента крупных сделок по
95-му процентилю, объём на рэндж-барах, подбор R под инструмент, глубина
окна свечей по ТФ (M1 — неделя).

**Урок вливания.** Двухфидовый прогон ДО вливания оказался невозможен:
демо-счёт FXCM даёт одно подключение к брокеру, и оно занято боевым фидом.
Двое суток обкатки на одной крипте пропустили баг, который живой форекс
нашёл за 30 секунд: `_sum_volume` проверял `key in source` (наличие ключа),
а sqlite возвращает ВСЕ колонки — у 103 тыс. свечей FXCM `delta` приходит
как NULL → None, и `0.0 + None` убивал каждое сообщение шины, задевавшее
старший ТФ (`d188cde`). Свечи, собранные в памяти, ключа не имеют вовсе —
поэтому тесты и обкатка молчали. Вывод на будущее: тестовый контур на одном
провайдере не проверяет код, общий для двух.

Замеры на 12 инструментах: тики 57 МБ/сутки (кольцо 14 дней → ~800 МБ),
БД выходит на полку ~25 МБ, профиль +11 МБ/год. Итого ~1 ГБ вместо
заложенных 2.5-3 ГБ.


Ключи **не нужны**: рыночные данные публичны. Только mainnet.

| Что           | Как                                                              |
|---------------|------------------------------------------------------------------|
| Список рынков | `OrderApi.order_books()` → symbol, market_id, decimals, min_base  |
| Карточка      | `OrderApi.order_book_details()` → last, 24h vol, OI, funding      |
| История       | `GET /api/v1/candles`, ≤500 за вызов, без auth                    |
| Live          | WS `wss://mainnet.zklighter.elliot.ai/stream`, канал `trade/{id}` |

### Проверенные факты (не перепроверять, не додумывать)

- `resolution` принимает **только**: `1m 5m 15m 1h 4h 1d`.
  Голые числа (`"1"`, `"60"`) → 400 `invalid param`.
- `t` — **миллисекунды**.
- На WS `price`/`size` — **строки**; в REST — числа.
  Парсить `float()` **по каждому полю отдельно**, единый тип не предполагать.
- Объём: `v` = base (BTC), `V` = quote (USDC). **Хранить оба.**
- `mark_price`/`index_price` есть в JSON, но **отсутствуют в Pydantic-модели SDK** →
  ходить **raw REST**, не через SDK-модели. SDK — только для подписи ордеров (Фаза 5).
- `count_back` ведёт себя непредсказуемо — **не использовать**. Только `start`/`end`
  + пагинация по 500.
- `daily_price_change` — доля, не проценты (× 100 для UI).
- Пинг: сервер шлёт `{"type":"ping"}`, надо отвечать `{"type":"pong"}`.
  Без ответа раз в ~2 минуты соединение рвётся.

### Решения

- **Белый список рынков:** BTC, ETH, SOL, HYPE. Остальные из 223 — лениво,
  по выбору пользователя. 223 WS-подписки не открывать.
- Фид отдаёт хабу **тики (сделки), не свечи**. Хаб нарезает их той же логикой,
  что и FXCM → все TF, включая секундные, бесплатно.
- REST-свечи — **только бэкфил** при старте: 2000 баров M1 = 4 запроса по 500.
- **S5–S30 бэкфила не имеют** (минимальное разрешение API = `1m`). Копятся живьём.
  Согласовано: владельцу история нужна «начиная от M1».
- **Запинить SDK на конкретный коммит** в терминале И в боте. Сейчас в боте стоит
  `git+https://github.com/elliottech/lighter-python.git` без тега — проекты могут
  молча разъехаться по версиям.

### Переиспользовать из проекта бота

- `src/collector/list_markets.py` — копируется как есть, зависимостей нет.
- `_normalize_trade()`, `Deduper`, ping/pong из `src/collector/lighter_ticks.py` →
  вынести в **общий модуль**, шарить между ботом и терминалом. **Не дублировать.**
- `MinuteCandleBuilder` из бота **не брать** — у хаба свой нарезатель.

### Фронт

Гистограмма объёма отдельной паной (LWC `HistogramSeries`), скрыта при
`has_volume = false`.

---

## Фаза 4 — 24/7  ✅ ЗАВЕРШЕНА (2026-07-18)

Возможна только после Фазы 1.

- Снять крон-остановку сервиса `chart` (была нужна, т.к. форекс закрыт по выходным;
  крипта торгуется всегда). FXCM-фид можно продолжать гасить — **хаб работает всегда**.
- Watchdog переподключения на каждый фид
- Heartbeat в WS клиентам
- Health-эндпоинт
- Structured-логи вместо `print`

---

## Фаза 5 — Торговая панель

- Отдельный процесс `trade_gateway.py`, свой systemd-юнит. Падение шлюза
  не роняет поток данных.
- **Ключи только на сервере.** В браузер не попадают никогда. Фронт шлёт намерение →
  шлюз подписывает и отправляет.
- Риск-лимиты на стороне сервера, не на фронте.
- Позиции/ордера на графике — переиспользовать `js/position-tool.js`.
- **Начинать с testnet** (`testnet.zklighter.elliot.ai`, chain_id 300).
  Mainnet (304) — только после отладки.

Технические детали Lighter: `SignerClient`, `account_index`, `api_private_keys`.
Подпись — нативный бинарник (ctypes `.so`), архитектура VPS должна совпасть.
Nonce-менеджер (режим OPTIMISTIC по умолчанию). Auth-токен живёт 10 минут.

**Открытый вопрос:** ключи и депозит на Lighter не настроены (в боте grep по
`SignerClient` — ноль совпадений). Нужны от владельца до начала фазы.

---

## Фаза 6 — Карточка инструмента

`order_book_details()` закрывает почти всё: last price, 24h volume, OI, funding,
daily high/low/change. Капитализация — внешний источник (CoinGecko), это уже
не про биржу.

---

## Фаза 7 — Брифинг: подпроект `briefing/` с памятью и контекстом  ✅ ОСНОВНОЕ ГОТОВО (2026-07-18)

**Слои 1-3 в проде.** Цель — не «куча умных слов», а брифинг, ПОНИМАЮЩИЙ контекст:
помнит вчера/неделю назад, сверяет прошлый прогноз с фактом, ведёт связную историю.
Выносится в отдельный модуль `briefing/` со своими инструкциями (`BRIEFING.md`).

### Диагностика движка (2026-07-18, замерено живьём)

Движок самодостаточен (`pre_session_brief.py` + `data_loaders/fetch_news.py`),
ничего не тянет из соседних проектов. Дефекты:
- **RSS:** 3 фида Reuters мертвы (URLError — Reuters закрыл публичные RSS).
  Лимит `feed.entries[:5]` режет выход втрое (8 релевантных вместо 23).
  Живы и проверены: Bloomberg (через 301), FT, WSJ, CNBC. Кандидаты (все 200,
  проверены): **FXStreet(30), ForexLive(25), Investing, ActionForex(20), MarketWatch**.
- **Календарь:** ForexFactory `ff_calendar_thisweek.json` (домен `nfs.faireconomy.media`)
  ЖИВОЙ (98 событий). `nextweek` даёт 404 (не существует) — убрать, спамит лог.
  «0 событий» в субботу — эффект выходных, не баг; в будни события есть.
- **Время:** везде `%H:%M UTC`, нужно **UTC+5** (время машины).
- **Память:** `briefing_context.json` уже хранит 5 наблюдений/пару и подставляет
  в промпт — но плоско, без структуры «сбылось/не сбылось».

### Решения (согласовано с владельцем)

- **Память = структурированный журнал + самооценка** (глубина 7-14 дней). Каждый
  брифинг сверяет прошлый прогноз с фактом из `market.db` (дошла цена до уровня?
  bias угадан?) → честность вместо самоуверенности, связный контекст, трек-рекорд.
- **Структура — отдельный модуль `briefing/`.**
- **Доставка/модель — пока как есть** (DeepSeek + панель). Telegram/модель — позже.
- **cheap-llm (DeepSeek MCP)** — использовать где оправдано (резюме/классификация),
  не для архитектуры и не в боевом пути без нужды.

### Целевая структура

```
briefing/
  sources.py    — RSS (живые + диагностика по каждому фиду) + календарь FF
  memory.py     — журнал брифингов + самооценка (сверка с market.db)
  technical.py  — техкартина из market.db (перенос как есть)
  prompt.py     — сборка промпта (техника+новости+память+самооценка)
  agent.py      — вызов DeepSeek (организация + скиллы)
  run.py        — точка входа (заменит pre_session_brief.py в кроне)
  BRIEFING.md   — инструкции подпроекта
data/briefing_journal.json  — структурированный журнал (заменит плоский context)
```

### Слои (каждый проверяем изолированно)

1. **Слой 1 — источники** (В РАБОТЕ): `briefing/sources.py` — живые фиды +
   диагностика по каждому + `feed.entries[:5]`→больше + календарь (убрать nextweek) +
   время UTC+5. Порог «новостей мало» в `briefing.json` для фронта.
2. **Слой 2 — память:** `briefing/memory.py` — журнал + самооценка.
3. **Слой 3 — сборка:** перенос техники/промпта/агента, переключение крона.
   `pre_session_brief.py` → `archive/`.

### Настройка под себя (7.2, позже — по факту использования)

Набор пар/сессий (сейчас 4×3), что нужно в карточке, Telegram, модель/стоимость.

---

## Сразу после вливания Фазы 3

Порядок согласован с владельцем 2026-07-19.

### 1. Объём и дельта — в систему индикаторов

**Сейчас сделано хуже, чем можно.** Гистограмма объёма зашита в график
жёстко, с тумблером в меню, хотя в проекте уже есть `js/indicators.js` с
`IndicatorManager`: панель выбора, настройки, сохранение в layout.

Причина обхода: `_teardownAllOscillatorSeries()` сносит все паны кроме
нулевой при каждой перестройке осцилляторов — отдельная пана объёма
умирала бы при добавлении любого RSI. Проблему обошли, а не решили.

Правильно — оверлейный индикатор (как SMA/EMA/Bollinger: рисуются поверх
свечей и перестройку осцилляторов переживают). Тогда объём получает то же,
что остальные: добавление по желанию, настройки цвета и прозрачности,
несколько экземпляров.

Побочная выгода: дельту можно сделать ОТДЕЛЬНЫМ индикатором-гистограммой
для тех, кому нужна величина, а не только цвет столбца.

Убрать после переноса: тумблер «Объём» в меню графика, `layout.showVolume`.

### 2. Секундные ТФ — строить из тиков, не хранить

Идея владельца, замерено 2026-07-19: секундные ТФ дают **58% всех свечей**
и при этом всего 2.8 часа истории (S5) при 336 часах тиков в архиве.

Построение 2000 баров S5 из тиков: **0.8 с по BTC**, доли секунды по
остальным — та же схема, что уже работает для рэндж-баров.

Выигрыш: минус половина таблицы candles и столько же памяти хаба (он
упирается в потолок ~106 МБ именно из-за окон), глубина S5 растёт в 120 раз.

Цена: задержка ~0.8 с при переключении на секундный ТФ; глубже 14 дней
(ротация тиков) секундные ТФ не построить никогда.

### 3. Карточка инструмента (Фаза 6)

Дёшево: `order_book_details()` уже дёргается для метаданных, данные
приходят и выбрасываются. Даёт last price, 24h volume, OI, **funding**,
дневной диапазон. Фандинг на перпах — самостоятельный сигнал.

### 4. Профиль объёма по торговым сессиям

Сейчас период — календарные сутки UTC. Для синтетики на традиционные рынки
это смешивает активную сессию с почти пустыми выходными (замер: XAG в
субботу 19% активных минуток против 76% в пятницу) и даёт ложные уровни.

---

## Хвосты (при случае)

- `TZ_OFFSET = 5` захардкожен во фронте
- Путь бота на VPS: systemd-юнит говорит `/opt/lighter-rangebar-scalper`,
  доки — `/root/projects/Litr_v_odnogo`. Проверить:
  `systemctl cat lighter-ticks | grep WorkingDirectory`
- `LIGHTER_API_BASE` в `.env.example` бота объявлена, но не читается — мёртвая
- Прокрутка влево за пределы окна в 2000 баров: доложить лениво из REST по
  требованию. Заложить в API хаба, реализовать позже.

---

## Правила (из CLAUDE.md, действуют)

- **НИКОГДА** не перезапускать `server.py` / сервис `chart` без явного разрешения
- Перед правкой живого файла — подтвердить у владельца
- Docstring к каждой Python-функции (summary, Args, Returns)
